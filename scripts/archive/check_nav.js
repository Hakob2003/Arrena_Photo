const { chromium } = require("playwright"); (async () => { const browser = await chromium.launch(); const page = await browser.newPage(); await page.goto("https://arrena-photo-frontend-o4xg.onrender.com/generate"); await page.waitForTimeout(3000); const html = await page.$eval("nav", el => el.innerHTML); console.log("NAV HTML:", html.substring(0, 1000)); await browser.close(); })();
