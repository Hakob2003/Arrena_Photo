const { chromium } = require("playwright"); (async () => { const browser = await chromium.launch(); const page = await browser.newPage(); await page.goto("https://arrena-photo-frontend-o4xg.onrender.com/generate"); await page.waitForTimeout(3000); await page.screenshot({ path: "prod_screenshot.png" }); const icons = await page.$$eval("nav svg", svgs => svgs.map(s => s.outerHTML)); console.log("SVGs:", icons.length); await browser.close(); })();
