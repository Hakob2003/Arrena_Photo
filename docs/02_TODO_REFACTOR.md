# 📋 СТРУКТУРИРОВАННЫЙ БЭКЛОГ РЕФАКТОРИНГА (200 ЗАДАЧ)

Этот документ содержит исчерпывающий бэклог, состоящий из 200 задач (разбитых по 7 спринтам). Каждая задача описана строго по 13 полям.

---

### SEC-001

- **Название**: Удаление эндпоинта `/auth/setup-admin`
- **Описание**: Критическое обновление периметра безопасности. Немедленное устранение вектора атаки, связанного с удаление эндпоинта `/auth/setup-admin`.
- **Почему это нужно**: Любой пользователь сети может вызвать GET `/auth/setup-admin` и сбросить/создать администратора с паролем `admin123`.
- **Файлы**: `apps/backend-api/src/auth/auth.controller.ts`, `apps/backend-api/src/auth/auth.service.ts`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Частично (LLM-агент может написать код, но миграция схемы БД и apply terraform должны быть мануальными)
- **Рекомендации по реализации**: Использовать криптостойкие алгоритмы. Категорически запрещено выводить детали ошибки (Stack Traces) в HTTP ответ.

**Критерии завершения (DoD)**:
☐ Найти и удалить декоратор `@Get('setup-admin')` и соответствующий метод в `AuthController`.

☐ В `AuthService` удалить логику хардкодного создания юзера `admin@arrena.ai`.

☐ Эндпоинт полностью недоступен (404 Not Found).

---

### SEC-002

- **Название**: Создание CLI-инструмента для сидирования администратора
- **Описание**: Критическое обновление периметра безопасности. Немедленное устранение вектора атаки, связанного с создание cli-инструмента для сидирования администратора.
- **Почему это нужно**: После удаления `/setup-admin` нет легитимного способа создать админа при деплое.
- **Файлы**: `packages/database/prisma/seed.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Использовать криптостойкие алгоритмы. Категорически запрещено выводить детали ошибки (Stack Traces) в HTTP ответ.

**Критерии завершения (DoD)**:
☐ Создать `seed.ts` для Prisma.

☐ Использовать пакет `dotenv` для чтения `process.env.SUPERADMIN_EMAIL` и `SUPERADMIN_PASSWORD`.

☐ Хэшировать пароль через `bcrypt.hash(password, 12)`.

☐ Сохранять администратора с `role = 'ADMIN'`.

☐ Команда `pnpm db:seed` создает супер-админа без хардкода в кодовой базе.

---

### SEC-003

- **Название**: Исключение Fallback-ключей для JWT
- **Описание**: Критическое обновление периметра безопасности. Немедленное устранение вектора атаки, связанного с исключение fallback-ключей для jwt.
- **Почему это нужно**: Использование `config.get('JWT_SECRET', 'super-secret')` позволяет взломать токены, если ENV переменная не прокинута.
- **Файлы**: `apps/backend-api/src/auth/auth.module.ts`, `apps/backend-api/src/auth/jwt.strategy.ts`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Использовать криптостойкие алгоритмы. Категорически запрещено выводить детали ошибки (Stack Traces) в HTTP ответ.

**Критерии завершения (DoD)**:
☐ Удалить второй аргумент (fallback) из всех вызовов `configService.get('JWT_SECRET')`.

☐ Приложение использует только секрет из ENV-окружения.

---

### SEC-004

- **Название**: Валидация наличия JWT секрета при старте приложения
- **Описание**: Критическое обновление периметра безопасности. Немедленное устранение вектора атаки, связанного с валидация наличия jwt секрета при старте приложения.
- **Почему это нужно**: Приложение может запуститься без JWT-ключа, что приведет к непредсказуемому поведению.
- **Файлы**: `apps/backend-api/src/main.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Использовать криптостойкие алгоритмы. Категорически запрещено выводить детали ошибки (Stack Traces) в HTTP ответ.

**Критерии завершения (DoD)**:
☐ В функции `bootstrap()` добавить проверку: если `JWT_SECRET` undefined или меньше 32 символов — вызывать `Logger.fatal()` и `process.exit(1)`.

☐ Сервер падает при запуске, если не настроен сильный секретный ключ.

---

### SEC-005

- **Название**: Переход с Query параметров на HttpOnly Cookies для OAuth
- **Описание**: Критическое обновление периметра безопасности. Немедленное устранение вектора атаки, связанного с переход с query параметров на httponly cookies для oauth.
- **Почему это нужно**: После логина через Google токен передается в URL `?token=...`, оседая в истории браузера.
- **Файлы**: `apps/backend-api/src/auth/auth.controller.ts`, `apps/frontend/lib/api.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 4h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Использовать криптостойкие алгоритмы. Категорически запрещено выводить детали ошибки (Stack Traces) в HTTP ответ.

**Критерии завершения (DoD)**:
☐ В `googleAuthCallback` использовать `res.cookie('access_token', token, { httpOnly: true, secure: true, sameSite: 'strict' })`.

☐ Убрать редирект с query-параметрами.

☐ Токен недоступен через JavaScript (защита от XSS), передается автоматически в заголовках.

---

### SEC-006

- **Название**: Защита от CSRF атак при использовании Cookies
- **Описание**: Критическое обновление периметра безопасности. Немедленное устранение вектора атаки, связанного с защита от csrf атак при использовании cookies.
- **Почему это нужно**: Переход на Cookies открывает вектор для Cross-Site Request Forgery (CSRF).
- **Файлы**: `apps/backend-api/src/main.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Использовать криптостойкие алгоритмы. Категорически запрещено выводить детали ошибки (Stack Traces) в HTTP ответ.

**Критерии завершения (DoD)**:
☐ Установить и настроить пакет `csurf` (или `@nestjs/csrf`).

☐ Настроить выдачу CSRF-токена для фронтенда.

☐ Все мутирующие методы (POST, PUT, DELETE) требуют `X-CSRF-Token`.

---

### SEC-007

- **Название**: Шифрование OAuth токенов провайдеров в базе данных
- **Описание**: Критическое обновление периметра безопасности. Немедленное устранение вектора атаки, связанного с шифрование oauth токенов провайдеров в базе данных.
- **Почему это нужно**: Токены сторонних провайдеров (Google, VK) хранятся как plain-text.
- **Файлы**: `apps/backend-api/src/common/utils/encryption.util.ts`, `auth.service.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 4h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Использовать криптостойкие алгоритмы. Категорически запрещено выводить детали ошибки (Stack Traces) в HTTP ответ.

**Критерии завершения (DoD)**:
☐ Написать `EncryptionUtil` на базе `crypto.createCipheriv` (алгоритм `aes-256-gcm`).

☐ Зашифровать все токены при записи в Prisma.

☐ В таблице `User` в колонках `providerAccessToken` лежат нечитаемые бинарные данные.

---

### SEC-008

- **Название**: Скрипт миграции старых паролей (Опционально)
- **Описание**: Критическое обновление периметра безопасности. Немедленное устранение вектора атаки, связанного с скрипт миграции старых паролей (опционально).
- **Почему это нужно**: Если ранее использовался слабый алгоритм хэширования, нужно перехэшировать.
- **Файлы**: `scripts/rehash-passwords.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Низкий
- **Влияние**: Локальное улучшение модуля rehash-passwords.ts`. Снижает технический долг в изолированной области.
- **Приоритет**: LOW
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Использовать криптостойкие алгоритмы. Категорически запрещено выводить детали ошибки (Stack Traces) в HTTP ответ.

**Критерии завершения (DoD)**:
☐ Написать скрипт, сканирующий старые пароли и просящий юзеров сбросить их при входе (или перехэширующий, если возможно).

☐ План перевода на сильный bcrypt.

---

### SEC-009

- **Название**: Ротация JWT Refresh токенов
- **Описание**: Критическое обновление периметра безопасности. Немедленное устранение вектора атаки, связанного с ротация jwt refresh токенов.
- **Почему это нужно**: Бесконечные или статичные refresh-токены могут быть украдены.
- **Файлы**: `apps/backend-api/src/auth/auth.service.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 5h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля auth.service.ts`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Использовать криптостойкие алгоритмы. Категорически запрещено выводить детали ошибки (Stack Traces) в HTTP ответ.

**Критерии завершения (DoD)**:
☐ При каждом использовании refresh-токена генерировать новую пару (Access + Refresh).

☐ Инвалидировать старый refresh-токен в базе данных.

☐ Украденный refresh-токен можно использовать только один раз (сработает защита Reuse Detection).

---

### SEC-010

- **Название**: Механизм принудительного отзыва сессий (Logout Everywhere)
- **Описание**: Критическое обновление периметра безопасности. Немедленное устранение вектора атаки, связанного с механизм принудительного отзыва сессий (logout everywhere).
- **Почему это нужно**: Пользователь не может выйти со всех устройств при подозрении на взлом.
- **Файлы**: `apps/backend-api/src/auth/auth.service.ts`, `User` Prisma model
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля auth.service.ts`, `User` Prisma model. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Использовать криптостойкие алгоритмы. Категорически запрещено выводить детали ошибки (Stack Traces) в HTTP ответ.

**Критерии завершения (DoD)**:
☐ Добавить поле `tokenVersion` (Int) в таблицу `User`.

☐ Вшивать `tokenVersion` в payload JWT.

☐ Если версия в токене не совпадает с БД — отклонять запрос. При "Logout Everywhere" делать `tokenVersion++`.

☐ Сброс пароля или нажатие кнопки "Выйти со всех устройств" моментально инвалидирует все выданные ранее JWT.

---

### SEC-011

- **Название**: Создание Role-Based Access Control (RBAC) Guard'а
- **Описание**: Критическое обновление периметра безопасности. Немедленное устранение вектора атаки, связанного с создание role-based access control (rbac) guard'а.
- **Почему это нужно**: В системе нет единого механизма проверки ролей, кроме примитивного `JwtAuthGuard`.
- **Файлы**: `apps/backend-api/src/auth/guards/roles.guard.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Использовать криптостойкие алгоритмы. Категорически запрещено выводить детали ошибки (Stack Traces) в HTTP ответ.

**Критерии завершения (DoD)**:
☐ Имплементировать `CanActivate` интерфейс для `RolesGuard`.

☐ Использовать `Reflector` для извлечения необходимых ролей из контекста декоратора `@Roles()`.

☐ Сравнивать `req.user.role` с требуемым массивом ролей.

☐ `RolesGuard` корректно блокирует запросы и возвращает HTTP 403 Forbidden.

---

### SEC-012

- **Название**: Создание декоратора `@Roles`
- **Описание**: Критическое обновление периметра безопасности. Немедленное устранение вектора атаки, связанного с создание декоратора `@roles`.
- **Почему это нужно**: Нет стандартного способа указать требуемую роль для маршрута.
- **Файлы**: `apps/backend-api/src/auth/decorators/roles.decorator.ts`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Использовать криптостойкие алгоритмы. Категорически запрещено выводить детали ошибки (Stack Traces) в HTTP ответ.

**Критерии завершения (DoD)**:
☐ Написать кастомный декоратор: `export const Roles = (...roles: RoleName[]) => SetMetadata('roles', roles);`.

☐ Декоратор доступен для импорта и поддерживает строгую типизацию из Prisma enum `RoleName`.

---

### SEC-013

- **Название**: Применение RolesGuard на Admin API
- **Описание**: Критическое обновление периметра безопасности. Немедленное устранение вектора атаки, связанного с применение rolesguard на admin api.
- **Почему это нужно**: В данный момент любой пользователь с токеном может делать запросы в админку.
- **Файлы**: `apps/backend-api/src/admin/admin.controller.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Использовать криптостойкие алгоритмы. Категорически запрещено выводить детали ошибки (Stack Traces) в HTTP ответ.

**Критерии завершения (DoD)**:
☐ Повесить `@UseGuards(JwtAuthGuard, RolesGuard)` на класс `AdminController`.

☐ Повесить `@Roles(RoleName.ADMIN)` на весь класс.

☐ Ни один эндпоинт админки не доступен для юзера с `RoleName.USER`.

---

### SEC-014

- **Название**: Применение RolesGuard на генерацию изображений (Бан система)
- **Описание**: Критическое обновление периметра безопасности. Немедленное устранение вектора атаки, связанного с применение rolesguard на генерацию изображений (бан система).
- **Почему это нужно**: Забаненные пользователи все еще могут вызывать генерации, если их JWT не истек.
- **Файлы**: `apps/backend-api/src/generations/generations.controller.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля generations.controller.ts`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Использовать криптостойкие алгоритмы. Категорически запрещено выводить детали ошибки (Stack Traces) в HTTP ответ.

**Критерии завершения (DoD)**:
☐ Добавить роль `BANNED` в Prisma.

☐ Запретить доступ к `GenerationsController` для пользователей с этой ролью.

☐ Роль `BANNED` мгновенно отрезает доступ к платному API.

---

### SEC-015

- **Название**: Настройка Rate Limiting (DDoS защита)
- **Описание**: Критическое обновление периметра безопасности. Немедленное устранение вектора атаки, связанного с настройка rate limiting (ddos защита).
- **Почему это нужно**: Отсутствие лимитов на количество HTTP-запросов (потенциальный DDoS или брутфорс).
- **Файлы**: `apps/backend-api/src/app.module.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Использовать криптостойкие алгоритмы. Категорически запрещено выводить детали ошибки (Stack Traces) в HTTP ответ.

**Критерии завершения (DoD)**:
☐ Установить `@nestjs/throttler`.

☐ Настроить глобальный лимит (например, 100 запросов в минуту с одного IP).

☐ При превышении лимита сервер возвращает `429 Too Many Requests`.

---

### SEC-016

- **Название**: Специфичный Rate Limit на эндпоинты аутентификации
- **Описание**: Критическое обновление периметра безопасности. Немедленное устранение вектора атаки, связанного с специфичный rate limit на эндпоинты аутентификации.
- **Почему это нужно**: Злоумышленник может брутфорсить `/login`, проверяя 10,000 паролей в секунду.
- **Файлы**: `apps/backend-api/src/auth/auth.controller.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Использовать криптостойкие алгоритмы. Категорически запрещено выводить детали ошибки (Stack Traces) в HTTP ответ.

**Критерии завершения (DoD)**:
☐ Настроить жесткий Throttler для `/login` и `/register` (не более 5 попыток в минуту с одного IP).

☐ Брутфорс паролей экономически и технически нецелесообразен.

---

### SEC-017

- **Название**: Внедрение Helmet (HTTP Security Headers)
- **Описание**: Критическое обновление периметра безопасности. Немедленное устранение вектора атаки, связанного с внедрение helmet (http security headers).
- **Почему это нужно**: Отсутствие базовых заголовков безопасности (X-XSS-Protection, Strict-Transport-Security).
- **Файлы**: `apps/backend-api/src/main.ts`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля main.ts`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Использовать криптостойкие алгоритмы. Категорически запрещено выводить детали ошибки (Stack Traces) в HTTP ответ.

**Критерии завершения (DoD)**:
☐ Установить пакет `helmet`.

☐ Подключить `app.use(helmet())` в `bootstrap()`.

☐ Инструменты сканирования уязвимостей выдают класс A для HTTP заголовков сервера.

---

### SEC-018

- **Название**: Настройка CORS Policy
- **Описание**: Критическое обновление периметра безопасности. Немедленное устранение вектора атаки, связанного с настройка cors policy.
- **Почему это нужно**: Сейчас CORS разрешает запросы с любых источников (`*`), что является уязвимостью.
- **Файлы**: `apps/backend-api/src/main.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Использовать криптостойкие алгоритмы. Категорически запрещено выводить детали ошибки (Stack Traces) в HTTP ответ.

**Критерии завершения (DoD)**:
☐ Настроить CORS: `origin: [process.env.FRONTEND_URL, 'http://localhost:3000']`.

☐ Разрешить только определенные методы (GET, POST, PUT, DELETE).

☐ Сторонние сайты не могут отправлять AJAX-запросы к нашему API от имени пользователя.

---

### SEC-019

- **Название**: Аудит зависимостей (NPM Audit)
- **Описание**: Критическое обновление периметра безопасности. Немедленное устранение вектора атаки, связанного с аудит зависимостей (npm audit).
- **Почему это нужно**: Сторонние npm-пакеты могут содержать известные CVE уязвимости.
- **Файлы**: `package.json`, CI/CD
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Низкий
- **Влияние**: Локальное улучшение модуля CD. Снижает технический долг в изолированной области.
- **Приоритет**: LOW
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Использовать криптостойкие алгоритмы. Категорически запрещено выводить детали ошибки (Stack Traces) в HTTP ответ.

**Критерии завершения (DoD)**:
☐ Настроить `pnpm audit` в рамках CI/CD пайплайна.

☐ Обновить устаревшие пакеты с критическими уязвимостями.

☐ В логах сборки отсутствуют предупреждения уровня High и Critical.

---

### SEC-020

- **Название**: Изоляция данных пользователей (Tenancy check)
- **Описание**: Критическое обновление периметра безопасности. Немедленное устранение вектора атаки, связанного с изоляция данных пользователей (tenancy check).
- **Почему это нужно**: Риск Insecure Direct Object Reference (IDOR). Если юзер передаст `generationId` чужой картинки в метод удаления, он может удалить чужую работу.
- **Файлы**: `apps/backend-api/src/generations/generations.service.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 4h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Использовать криптостойкие алгоритмы. Категорически запрещено выводить детали ошибки (Stack Traces) в HTTP ответ.

**Критерии завершения (DoD)**:
☐ Провести ревью всех методов `delete`, `update`, `findOne`.

☐ Убедиться, что везде стоит `where: { id: dto.id, userId: req.user.id }`.

☐ 100% гарантия, что юзер не имеет доступа к записями других юзеров по прямым ID.

---

### WRK-021

- **Название**: Настройка NestJS Monorepo структуры
- **Описание**: Экстремальная архитектурная оптимизация подсистемы вычислений. Кардинальное снижение нагрузки на Event Loop и дисковый I/O.
- **Почему это нужно**: Текущая структура не предназначена для нескольких самостоятельных приложений.
- **Файлы**: `nest-cli.json`, `apps/`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Выполнить конфигурацию `nest-cli.json` для поддержки monorepo mode.

☐ Настроить директорию `apps/worker`.

☐ `pnpm run build` успешно компилирует оба приложения по отдельности.

---

### WRK-022

- **Название**: Создание точки входа для Worker'а
- **Описание**: Экстремальная архитектурная оптимизация подсистемы вычислений. Кардинальное снижение нагрузки на Event Loop и дисковый I/O.
- **Почему это нужно**: Воркер должен стартовать без прослушивания HTTP-порта, чтобы не конфликтовать с основным API.
- **Файлы**: `apps/worker/src/main.ts`, `apps/worker/src/worker.module.ts`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Создать `main.ts` использующий `createMicroservice` (или просто `createApplicationContext`).

☐ Подключить `BullModule.forRoot()` с доступом к Redis.

☐ Процесс висит в фоне и не биндит порт 4000.

---

### WRK-023

- **Название**: Вынос BullMQ Consumer в пакет Worker
- **Описание**: Экстремальная архитектурная оптимизация подсистемы вычислений. Кардинальное снижение нагрузки на Event Loop и дисковый I/O.
- **Почему это нужно**: Консьюмер (потребитель задач) находится в том же модуле, что и веб-сервер.
- **Файлы**: `apps/worker/src/processors/generation.processor.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Переместить декоратор `@Processor('generations')` из `backend-api` в `worker`.

☐ Убедиться, что `backend-api` теперь только добавляет задачи (`@InjectQueue`).

☐ При старте API без воркера, задачи накапливаются в Redis. При старте воркера — они мгновенно разгребаются.

---

### WRK-024

- **Название**: Разделение зависимостей (OpenAI / Replicate)
- **Описание**: Экстремальная архитектурная оптимизация подсистемы вычислений. Кардинальное снижение нагрузки на Event Loop и дисковый I/O.
- **Почему это нужно**: Тяжелые SDK для генерации загружаются в память HTTP-сервера.
- **Файлы**: `apps/worker/src/providers/`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 4h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Переместить классы `OpenAIProvider` и `ReplicateProvider` исключительно в модуль Воркера.

☐ API серверу оставить только контракты (DTO).

☐ В `apps/backend-api/package.json` удалены библиотеки провайдеров.

---

### WRK-025

- **Название**: Устранение блокирующего setTimeout
- **Описание**: Экстремальная архитектурная оптимизация подсистемы вычислений. Кардинальное снижение нагрузки на Event Loop и дисковый I/O.
- **Почему это нужно**: Строка `await new Promise(r => setTimeout(r, 30000))` полностью блокирует V8 Thread воркера.
- **Файлы**: `apps/worker/src/processors/generation.processor.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Удалить `setTimeout` из логики обработки (`process()`).

☐ Воркер обрабатывает задачи мгновенно, без искусственных зависаний потока.

---

### WRK-026

- **Название**: Умный delay на стороне Producer'а
- **Описание**: Экстремальная архитектурная оптимизация подсистемы вычислений. Кардинальное снижение нагрузки на Event Loop и дисковый I/O.
- **Почему это нужно**: Удаление `setTimeout` (WRK-025) убрало задержку для Free юзеров. Ее нужно вернуть грамотно.
- **Файлы**: `apps/backend-api/src/generations/generations.service.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ При вызове `this.queue.add()`, передавать параметр `{ delay: 30000 }`, если тариф юзера `free`.

☐ BullMQ самостоятельно удерживает задачу в Redis 30 секунд, прежде чем отдать её воркеру.

---

### WRK-027

- **Название**: Настройка Retry & Exponential Backoff
- **Описание**: Экстремальная архитектурная оптимизация подсистемы вычислений. Кардинальное снижение нагрузки на Event Loop и дисковый I/O.
- **Почему это нужно**: Если OpenAI API отвечает `503 Service Unavailable`, задача падает и помечается как `FAILED`. Деньги юзера сгорают.
- **Файлы**: `apps/backend-api/src/generations/generations.service.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Настроить в BullMQ: `attempts: 3`, `backoff: { type: 'exponential', delay: 2000 }`.

☐ При сетевой ошибке воркер автоматически повторяет запрос через 2с, затем через 4с, затем через 8с.

---

### WRK-028

- **Название**: Обработка статуса FAILED с возвратом средств
- **Описание**: Экстремальная архитектурная оптимизация подсистемы вычислений. Кардинальное снижение нагрузки на Event Loop и дисковый I/O.
- **Почему это нужно**: Если задача падает окончательно (3 попытки исчерпаны), списанные кредиты не возвращаются пользователю.
- **Файлы**: `apps/worker/src/processors/generation.processor.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 4h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Использовать хук `@OnQueueFailed()` в воркере (или блок `catch` при последней попытке).

☐ Делать вызов сервиса биллинга для возврата (Refund) потраченных кредитов.

☐ Юзер не теряет кредиты, если генерация упала по вине сервера или провайдера.

---

### WRK-029

- **Название**: Имплементация Graceful Shutdown для Воркера
- **Описание**: Экстремальная архитектурная оптимизация подсистемы вычислений. Кардинальное снижение нагрузки на Event Loop и дисковый I/O.
- **Почему это нужно**: При деплое Kubernetes убивает процесс SIGTERM сигналом, прерывая активную генерацию картинки.
- **Файлы**: `apps/worker/src/main.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Настроить перехват `SIGTERM`.

☐ Вызывать `app.close()`. BullMQ автоматически дождется завершения активных джобов перед выходом.

☐ Перезапуск контейнера не обрывает генерации на полпути.

---

### WRK-030

- **Название**: Раздельные Docker-контейнеры
- **Описание**: Экстремальная архитектурная оптимизация подсистемы вычислений. Кардинальное снижение нагрузки на Event Loop и дисковый I/O.
- **Почему это нужно**: Сейчас все крутится в одном контейнере.
- **Файлы**: `docker-compose.prod.yml`, `apps/worker/Dockerfile`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Создать отдельный `Dockerfile` (или таргет в multi-stage) для `worker`.

☐ Добавить `ai-worker` сервис в `docker-compose`.

☐ Команда `docker-compose up --scale ai-worker=3` поднимает 1 API сервер и 3 воркера.

---

### DB-031

- **Название**: Развертывание MinIO в инфраструктуре
- **Описание**: Агрессивная миграция схемы данных и оптимизация слоя персистентности (Prisma ORM). Разгрузка PostgreSQL и пулеров коннектов.
- **Почему это нужно**: Отсутствие объектного хранилища S3.
- **Файлы**: `docker-compose.prod.yml`, `.env.example`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Любую миграцию схемы выполнять строго в два этапа (Expand and Contract паттерн), чтобы полностью исключить блокировки таблицы (Table Locks) и обеспечить Zero-Downtime.

**Критерии завершения (DoD)**:
☐ Добавить MinIO в compose, задать креды (Root User/Password), настроить порты 9000 (API) и 9001 (Console).

☐ MinIO доступен на `localhost:9000`.

---

### DB-032

- **Название**: Создание модуля Storage в NestJS
- **Описание**: Агрессивная миграция схемы данных и оптимизация слоя персистентности (Prisma ORM). Разгрузка PostgreSQL и пулеров коннектов.
- **Почему это нужно**: Нет кода для общения с S3 API.
- **Файлы**: `apps/worker/src/storage/storage.module.ts`, `storage.service.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Любую миграцию схемы выполнять строго в два этапа (Expand and Contract паттерн), чтобы полностью исключить блокировки таблицы (Table Locks) и обеспечить Zero-Downtime.

**Критерии завершения (DoD)**:
☐ Интегрировать библиотеку `minio` или `@aws-sdk/client-s3`. Создать метод `uploadImage(buffer: Buffer, filename: string)`.

☐ Метод успешно загружает Buffer в bucket и возвращает валидный public URL.

---

### DB-033

- **Название**: Добавление s3ImageUrl в Prisma
- **Описание**: Агрессивная миграция схемы данных и оптимизация слоя персистентности (Prisma ORM). Разгрузка PostgreSQL и пулеров коннектов.
- **Почему это нужно**: База не готова к новым URL.
- **Файлы**: `packages/database/prisma/schema.prisma`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Любую миграцию схемы выполнять строго в два этапа (Expand and Contract паттерн), чтобы полностью исключить блокировки таблицы (Table Locks) и обеспечить Zero-Downtime.

**Критерии завершения (DoD)**:
☐ Добавить поле `s3ImageUrl String?` в модель `Generation`. Создать миграцию `prisma migrate dev`.

☐ В таблице появилась новая колонка.

---

### DB-034

- **Название**: Настройка паттерна Dual-Write в Воркере
- **Описание**: Агрессивная миграция схемы данных и оптимизация слоя персистентности (Prisma ORM). Разгрузка PostgreSQL и пулеров коннектов.
- **Почему это нужно**: Переход на S3 должен быть бесшовным.
- **Файлы**: `apps/worker/src/processors/generation.processor.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 4h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Любую миграцию схемы выполнять строго в два этапа (Expand and Contract паттерн), чтобы полностью исключить блокировки таблицы (Table Locks) и обеспечить Zero-Downtime.

**Критерии завершения (DoD)**:
☐ Изменить логику сохранения. Писать `Base64` в `resultImage` и S3 ссылку в `s3ImageUrl` одновременно.

☐ При генерации новой картинки заполняются обе колонки.

---

### DB-035

- **Название**: Модификация API для чтения из S3 (Read-Repair)
- **Описание**: Агрессивная миграция схемы данных и оптимизация слоя персистентности (Prisma ORM). Разгрузка PostgreSQL и пулеров коннектов.
- **Почему это нужно**: Фронтенд все еще читает старую колонку.
- **Файлы**: `apps/backend-api/src/generations/generations.service.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Любую миграцию схемы выполнять строго в два этапа (Expand and Contract паттерн), чтобы полностью исключить блокировки таблицы (Table Locks) и обеспечить Zero-Downtime.

**Критерии завершения (DoD)**:
☐ В методе, возвращающем историю (Feed), отдавать приоритет S3: `imageUrl: gen.s3ImageUrl || gen.resultImage`.

☐ Если есть S3 ссылка, фронтенд рендерит ее. Иначе — Base64.

---

### DB-036

- **Название**: Скрипт миграции исторических данных
- **Описание**: Агрессивная миграция схемы данных и оптимизация слоя персистентности (Prisma ORM). Разгрузка PostgreSQL и пулеров коннектов.
- **Почему это нужно**: Сотни мегабайт старых Base64 все еще лежат в БД, перегружая TOAST.
- **Файлы**: `scripts/migrate-base64.ts`
- **Зависимости**: Нет
- **Сложность**: Extreme (Требует пары сеньор-разработчиков)
- **Оценка времени**: 8h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Любую миграцию схемы выполнять строго в два этапа (Expand and Contract паттерн), чтобы полностью исключить блокировки таблицы (Table Locks) и обеспечить Zero-Downtime.

**Критерии завершения (DoD)**:
☐ Написать скрипт, сканирующий `resultImage` порциями по 100 записей.

☐ Декодировать Base64 в Buffer -> Заливать в S3 -> Обновлять запись в БД.

☐ Выводить прогресс-бар в консоль.

☐ Скрипт успешно отработал, в базе 0 записей без `s3ImageUrl`.

---

### DB-037

- **Название**: Nullify миграция (Удаление старых данных)
- **Описание**: Агрессивная миграция схемы данных и оптимизация слоя персистентности (Prisma ORM). Разгрузка PostgreSQL и пулеров коннектов.
- **Почему это нужно**: Записи скопированы, но старые мегабайты все еще занимают место.
- **Файлы**: `scripts/nullify-base64.sql`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Частично (LLM-агент может написать код, но миграция схемы БД и apply terraform должны быть мануальными)
- **Рекомендации по реализации**: Любую миграцию схемы выполнять строго в два этапа (Expand and Contract паттерн), чтобы полностью исключить блокировки таблицы (Table Locks) и обеспечить Zero-Downtime.

**Критерии завершения (DoD)**:
☐ Выполнить SQL: `UPDATE "Generation" SET "resultImage" = NULL WHERE "s3ImageUrl" IS NOT NULL`.

☐ Колонка `resultImage` стала пустой у 100% записей.

---

### DB-038

- **Название**: Запуск VACUUM FULL в PostgreSQL
- **Описание**: Агрессивная миграция схемы данных и оптимизация слоя персистентности (Prisma ORM). Разгрузка PostgreSQL и пулеров коннектов.
- **Почему это нужно**: После `UPDATE = NULL` место на диске не освободилось (Postgres оставляет "мертвые кортежи").
- **Файлы**: Terminal
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля Terminal. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Любую миграцию схемы выполнять строго в два этапа (Expand and Contract паттерн), чтобы полностью исключить блокировки таблицы (Table Locks) и обеспечить Zero-Downtime.

**Критерии завершения (DoD)**:
☐ Запустить `VACUUM FULL "Generation"`. Внимание: блокирует таблицу! Выполнять ночью.

☐ Размер файла БД на диске уменьшился до нормальных значений (килобайты вместо гигабайт).

---

### DB-039

- **Название**: Удаление колонки resultImage из Prisma
- **Описание**: Агрессивная миграция схемы данных и оптимизация слоя персистентности (Prisma ORM). Разгрузка PostgreSQL и пулеров коннектов.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `packages/database/prisma/schema.prisma`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля schema.prisma`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Частично (LLM-агент может написать код, но миграция схемы БД и apply terraform должны быть мануальными)
- **Рекомендации по реализации**: Любую миграцию схемы выполнять строго в два этапа (Expand and Contract паттерн), чтобы полностью исключить блокировки таблицы (Table Locks) и обеспечить Zero-Downtime.

**Критерии завершения (DoD)**:
☐ Удалить старое поле `resultImage`. Создать и применить миграцию.

☐ Поле физически отсутствует в схеме.

---

### DB-040

- **Название**: Оптимизация схемы: Индексы сортировки
- **Описание**: Агрессивная миграция схемы данных и оптимизация слоя персистентности (Prisma ORM). Разгрузка PostgreSQL и пулеров коннектов.
- **Почему это нужно**: Запрос `ORDER BY createdAt DESC` сканирует всю таблицу целиком.
- **Файлы**: `packages/database/prisma/schema.prisma`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Любую миграцию схемы выполнять строго в два этапа (Expand and Contract паттерн), чтобы полностью исключить блокировки таблицы (Table Locks) и обеспечить Zero-Downtime.

**Критерии завершения (DoD)**:
☐ Добавить `@@index([createdAt(sort: Desc)])`.

☐ Ускорение выборок ленты в 10-100 раз при больших объемах данных.

---

### DB-041

- **Название**: Настройка Foreign Keys (OnDelete Cascade)
- **Описание**: Агрессивная миграция схемы данных и оптимизация слоя персистентности (Prisma ORM). Разгрузка PostgreSQL и пулеров коннектов.
- **Почему это нужно**: Удаление пользователя невозможно, так как висят связанные транзакции и генерации.
- **Файлы**: `packages/database/prisma/schema.prisma`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Любую миграцию схемы выполнять строго в два этапа (Expand and Contract паттерн), чтобы полностью исключить блокировки таблицы (Table Locks) и обеспечить Zero-Downtime.

**Критерии завершения (DoD)**:
☐ Установить `onDelete: Cascade` для таблиц `Generation`, `Transaction`, `Watermark`.

☐ `prisma db push` успешно применяется, удаление юзера автоматически подчищает его историю.

---

### DB-042

- **Название**: Создание индекса для запросов по пользователю
- **Описание**: Агрессивная миграция схемы данных и оптимизация слоя персистентности (Prisma ORM). Разгрузка PostgreSQL и пулеров коннектов.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `packages/database/prisma/schema.prisma`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Любую миграцию схемы выполнять строго в два этапа (Expand and Contract паттерн), чтобы полностью исключить блокировки таблицы (Table Locks) и обеспечить Zero-Downtime.

**Критерии завершения (DoD)**:
☐ В таблицу `Generation` добавить `@@index([userId, createdAt(sort: Desc)])`.

☐ Вкладка "Моя история" на клиенте открывается мгновенно даже при 100 000+ генерациях в БД.

---

### DB-043

- **Название**: Внедрение Soft Delete (Опционально)
- **Описание**: Агрессивная миграция схемы данных и оптимизация слоя персистентности (Prisma ORM). Разгрузка PostgreSQL и пулеров коннектов.
- **Почему это нужно**: Случайное удаление данных нельзя восстановить.
- **Файлы**: `packages/database/prisma/schema.prisma`, `backend-api`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля schema.prisma`, `backend-api`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Любую миграцию схемы выполнять строго в два этапа (Expand and Contract паттерн), чтобы полностью исключить блокировки таблицы (Table Locks) и обеспечить Zero-Downtime.

**Критерии завершения (DoD)**:
☐ Добавить поле `deletedAt DateTime?`. Изменить логику сервисов, чтобы они использовали `where: { deletedAt: null }`.

☐ Записи не удаляются физически, а просто помечаются флагом.

---

### TYP-044

- **Название**: Инициализация Workspace пакета shared-types
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `packages/shared-types/package.json`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `packages/shared-types/package.json`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Да (через ESLint --fix / автоматизированный TS compiler refactoring)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Создать `package.json` с именем `@arrena-photo/shared-types`. Настроить `main` на `dist/index.js` и `types` на `dist/index.d.ts`.

☐ Пакет виден из корня через `pnpm -w ls`.

---

### TYP-045

- **Название**: Настройка tsconfig.json для shared-types
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `packages/shared-types/tsconfig.json`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `packages/shared-types/tsconfig.json`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Да (через ESLint --fix / автоматизированный TS compiler refactoring)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Настроить `compilerOptions`: `declaration: true`, `outDir: "./dist"`, `strict: true`.

☐ Команда `tsc -b` успешно компилирует типы в папку `dist`.

---

### TYP-046

- **Название**: Экспорт Enum'ов из базы данных в shared-types
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `packages/shared-types/src/enums.ts`.
- **Почему это нужно**: Фронтенд дублирует строки "PENDING", "COMPLETED" руками.
- **Файлы**: `packages/shared-types/src/enums.ts`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Да (через ESLint --fix / автоматизированный TS compiler refactoring)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Экспортировать `GenerationStatus`, `RoleName`, `TransactionStatus`.

☐ Фронтенд использует Enum'ы из пакета, опечатки невозможны.

---

### TYP-047

- **Название**: Типизация запроса CreateGenerationRequest
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `packages/shared-types/src/requests/generation.request.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `packages/shared-types/src/requests/generation.request.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Да (через ESLint --fix / автоматизированный TS compiler refactoring)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Описать интерфейс входящих данных для `/generations` (prompt, aspectRatio, modelId).

☐ Строгое понимание на фронте, какие ключи можно отправлять.

---

### TYP-048

- **Название**: Типизация ответа GenerationResponse
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `packages/shared-types/src/responses/generation.response.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `packages/shared-types/src/responses/generation.response.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Да (через ESLint --fix / автоматизированный TS compiler refactoring)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Описать структуру возвращаемых данных. Не возвращать приватные поля юзера (пароли и тд).

☐ Фронтенд точно знает, какие поля вернутся с бэкенда.

---

### TYP-049

- **Название**: Интеграция shared-types в frontend
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/package.json`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/frontend/package.json`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Да (через ESLint --fix / автоматизированный TS compiler refactoring)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Добавить `"@arrena-photo/shared-types": "workspace:*"` в зависимости. Выполнить `pnpm i`.

☐ Фронтенд успешно резолвит пакет.

---

### TYP-050

- **Название**: Интеграция shared-types в backend-api
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/package.json`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/backend-api/package.json`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Да (через ESLint --fix / автоматизированный TS compiler refactoring)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Добавить `"@arrena-photo/shared-types": "workspace:*"` в зависимости бэкенда.

☐ Бэкенд успешно резолвит пакет.

---

### TYP-051

- **Название**: Внедрение ValidationPipe на Бэкенде
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/main.ts`.
- **Почему это нужно**: Сейчас контроллеры принимают что угодно, и это летит в базу.
- **Файлы**: `apps/backend-api/src/main.ts`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Да (через ESLint --fix / автоматизированный TS compiler refactoring)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Настроить глобальный pipe: `new ValidationPipe({ whitelist: true, forbidNonWhitelisted: true })`.

☐ Любые лишние поля в payload будут автоматически отбрасываться сервером или возвращать 400.

---

### TYP-052

- **Название**: Создание CreateGenerationDto с валидацией
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/generations/dto/create-generation.dto.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/backend-api/src/generations/dto/create-generation.dto.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Да (через ESLint --fix / автоматизированный TS compiler refactoring)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Имплементировать интерфейс `CreateGenerationRequest` в виде класса. Добавить `@IsString()`, `@MaxLength(1000)` для поля `prompt`.

☐ Запрос с prompt > 1000 символов возвращает ошибку 400.

---

### TYP-053

- **Название**: Создание DTO для логина и регистрации
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/auth/dto/*.dto.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/backend-api/src/auth/dto/*.dto.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Да (через ESLint --fix / автоматизированный TS compiler refactoring)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Описать `RegisterDto` (email, password) с `@IsEmail()` и `@MinLength(8)`. Описать `LoginDto`.

☐ Регистрация с паролем '123' невозможна на уровне фреймворка.

---

### TYP-054

- **Название**: Типизация JwtPayload
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `packages/shared-types/src/types/jwt.type.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `packages/shared-types/src/types/jwt.type.ts`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Да (через ESLint --fix / автоматизированный TS compiler refactoring)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Создать интерфейс для внутренностей токена `{ id, email, role }`.

☐ Декодер токенов на фронте возвращает типизированный объект.

---

### TYP-055

- **Название**: Рефакторинг декоратора @CurrentUser
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/auth/decorators/current-user.decorator.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/backend-api/src/auth/decorators/current-user.decorator.ts`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Да (через ESLint --fix / автоматизированный TS compiler refactoring)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Удалить `any`, типизировать возврат декоратора интерфейсом `JwtPayload`.

☐ В контроллерах TS подсказывает автокомплитом поля юзера.

---

### TYP-056

- **Название**: Внедрение Swagger в NestJS
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/main.ts`.
- **Почему это нужно**: Нет документации API для фронтенд разработчиков.
- **Файлы**: `apps/backend-api/src/main.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Да (через ESLint --fix / автоматизированный TS compiler refactoring)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Установить `@nestjs/swagger`. Настроить `DocumentBuilder` и запустить на `/api/docs`.

☐ При переходе на localhost:4000/api/docs открывается интерфейс Swagger.

---

### TYP-057

- **Название**: Разметка контроллеров Swagger декораторами
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/**/*.controller.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/backend-api/src/**/*.controller.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля *.controller.ts`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Да (через ESLint --fix / автоматизированный TS compiler refactoring)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Добавить `@ApiTags()`, `@ApiOperation()`, `@ApiBearerAuth()` ко всем контроллерам.

☐ В Swagger красиво сгруппированы эндпоинты по разделам (Auth, Generations, Admin).

---

### TYP-058

- **Название**: Разметка DTO Swagger декораторами
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/**/*.dto.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/backend-api/src/**/*.dto.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля *.dto.ts`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Да (через ESLint --fix / автоматизированный TS compiler refactoring)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Использовать `@ApiProperty({ example: 'test@mail.com' })` для всех DTO.

☐ Swagger генерирует правильные JSON-схемы для примеров запросов.

---

### TYP-059

- **Название**: Полная очистка кодовой базы от `any` (Бэкенд)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/**/*.ts`.
- **Почему это нужно**: В проекте все еще остаются фрагменты с `any` (особенно в сервисах интеграций).
- **Файлы**: `apps/backend-api/src/**/*.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 4h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Да (через ESLint --fix / автоматизированный TS compiler refactoring)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Пройтись поиском по `: any` и заменить на конкретные интерфейсы.

☐ Команда `tsc --noEmit --strict` не выдает ошибок.

---

### TYP-060

- **Название**: Типизация Axios клиента на Фронтенде
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/lib/api.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/frontend/lib/api.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Да (через ESLint --fix / автоматизированный TS compiler refactoring)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Переписать методы `api.post()`, `api.get()` с использованием дженериков: `api.post<GenerationResponse>(...)`.

☐ В UI компонентах переменная `data` строго типизирована (отсутствует `any`).

---

### FE-061

- **Название**: Удаление глобального `'use client'`
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/app/layout.tsx`.
- **Почему это нужно**: Все страницы рендерятся на клиенте, убивая преимущества Next.js 14.
- **Файлы**: `apps/frontend/app/layout.tsx`, `apps/frontend/app/page.tsx`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Частично (LLM-агент может написать код, но миграция схемы БД и apply terraform должны быть мануальными)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Убрать `'use client'` с корневых файлов. Провайдеры (Query, Redux) вынести в отдельный `<Providers>` компонент с `'use client'`.

☐ Корневой Layout — серверный компонент.

---

### FE-062

- **Название**: Рефакторинг главной страницы `generate/page.tsx` (Шаг 1)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/components/generation/PromptSettings.tsx`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/frontend/components/generation/PromptSettings.tsx`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Выделить логику ввода промпта, выбора модели и aspect-ratio в Dumb Component.

☐ Компонент полностью независим и принимает состояние через props.

---

### FE-063

- **Название**: Рефакторинг главной страницы (Шаг 2)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/components/generation/ResultViewer.tsx`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/frontend/components/generation/ResultViewer.tsx`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Вынести область отображения картинки (Skeleton, Image, Error Message) в отдельный компонент.

☐ Компонент инкапсулирует логику показа результатов.

---

### FE-064

- **Название**: Создание хука `useGenerate`
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/hooks/useGenerate.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/frontend/hooks/useGenerate.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 4h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Перенести весь код вызова API, обработки ошибок, запуска поллинга статуса в этот кастомный хук.

☐ Страница `generate/page.tsx` импортирует 3-4 компонента и хук, становясь читаемой (менее 100 строк).

---

### FE-065

- **Название**: Рефакторинг Zustand Auth Store
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/store/auth.store.ts`.
- **Почему это нужно**: Токен хранится в localStorage, что вызывает FOUC при загрузке.
- **Файлы**: `apps/frontend/store/auth.store.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 4h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Убрать persist middleware для токена. Читать юзера из запроса к `/api/v1/auth/me` при старте приложения (или из серверных кук).

☐ Токен лежит только в HTTPOnly cookie.

---

### FE-066

- **Название**: Настройка Next.js Middleware для приватных роутов
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/middleware.ts`.
- **Почему это нужно**: Защищенные роуты рендерятся на клиенте перед редиректом.
- **Файлы**: `apps/frontend/middleware.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Написать middleware, проверяющий наличие куки `access_token`. Если нет — редирект `NextResponse.redirect(new URL('/login', request.url))`. Защитить роуты `/generate`, `/profile`, `/billing`.

☐ Попытка зайти на `/generate` инкогнито мгновенно кидает на логин без загрузки JS.

---

### FE-067

- **Название**: SSR на странице Маркетплейса шаблонов
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/app/marketplace/page.tsx`.
- **Почему это нужно**: Каталог шаблонов подгружается аяксом, не индексируется Google.
- **Файлы**: `apps/frontend/app/marketplace/page.tsx`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 4h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Сделать страницу асинхронной: `export default async function Marketplace()`. Делать `fetch` на сервере.

☐ `curl` запрос страницы маркетплейса возвращает HTML с заполненным списком шаблонов.

---

### FE-068

- **Название**: SSR для публичной страницы отдельной генерации
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/app/g/[id]/page.tsx`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/frontend/app/g/[id]/page.tsx`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Имплементировать страницу `/g/[id]` (расшаренная генерация). Рендерить картинку, промпт и автора на сервере.

☐ Готовая страница для шеринга в соцсети.

---

### FE-069

- **Название**: Оптимизация шрифтов (Next Font)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/app/layout.tsx`.
- **Почему это нужно**: Шрифты грузятся через внешний CSS, вызывая сдвиг макета (Layout Shift).
- **Файлы**: `apps/frontend/app/layout.tsx`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля layout.tsx`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Использовать `next/font/google`. Загружать шрифты на сервере и прокидывать CSS-переменные.

☐ Показатель CLS (Cumulative Layout Shift) в Lighthouse равен 0.

---

### FE-070

- **Название**: Замена тегов `<img>` на `<Image>`
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/components/**/*.tsx`.
- **Почему это нужно**: Изображения не сжимаются.
- **Файлы**: `apps/frontend/components/**/*.tsx`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Заменить `<img>` на `next/image` с настроенным доменом `minio`/`s3`.

☐ Оптимизация LCP (Largest Contentful Paint) в Lighthouse.

---

### FE-071

- **Название**: Настройка динамических Meta-тегов (OpenGraph)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/app/g/[id]/page.tsx`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/frontend/app/g/[id]/page.tsx`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Добавить экспорт функции `generateMetadata({ params })`. Формировать `og:title`, `og:image` на основе данных из БД.

☐ Отправка ссылки в Telegram отрисовывает превью именно с этой сгенерированной картинкой.

---

### FE-072

- **Название**: Внедрение Sitemap.xml генератора
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/app/sitemap.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/frontend/app/sitemap.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля sitemap.ts`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Написать серверный генератор, который отдает статические роуты и динамические роуты публичных маркетплейс-шаблонов.

☐ Файл доступен по адресу `/sitemap.xml` и соответствует формату XML.

---

### FE-073

- **Название**: Внедрение Robots.txt генератора
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/app/robots.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/frontend/app/robots.ts`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Низкий
- **Влияние**: Локальное улучшение модуля robots.ts`. Снижает технический долг в изолированной области.
- **Приоритет**: LOW
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Закрыть от индексации `/profile`, `/billing`, `/admin`. Разрешить `/` и `/marketplace`.

☐ Доступен корректный `/robots.txt`.

---

### FE-074

- **Название**: Рефакторинг файлов i18n (Локализация)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/locales/`.
- **Почему это нужно**: Захардкоженный текст в компонентах на русском или английском.
- **Файлы**: `apps/frontend/locales/`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 4h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Просканировать все UI компоненты. Перенести весь текст в словари (`ru.json`, `en.json`, `hy.json`).

☐ В `return (<div>...</div>)` компонентов нет "сырого" текста, только вызовы `t('key')`.

---

### FE-075

- **Название**: Исправление Viewport для a11y (Доступность)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/app/layout.tsx`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/frontend/app/layout.tsx`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля layout.tsx`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Удалить `maximum-scale=1` и `user-scalable=no` из настроек Viewport.

☐ Пользователи с нарушениями зрения могут делать зум страницы на смартфонах.

---

### FE-076

- **Название**: Обработка 404 (Not Found)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/app/not-found.tsx`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/frontend/app/not-found.tsx`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Низкий
- **Влияние**: Локальное улучшение модуля not-found.tsx`. Снижает технический долг в изолированной области.
- **Приоритет**: LOW
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Создать кастомную и красивую страницу 404 в стиле приложения (с кнопкой "На главную").

☐ Переход на несуществующий URL не показывает дефолтную страницу Next.js.

---

### FE-077

- **Название**: Обработка 500 (Server Error) на клиенте
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/app/error.tsx`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/frontend/app/error.tsx`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля error.tsx`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Создать кастомную Error Boundary с кнопкой "Try again".

☐ Фатальная ошибка рендера не крашит весь сайт белым экраном.

---

### FE-078

- **Название**: Удаление лишних CSS зависимостей (Tailwind Cleanup)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `tailwind.config.js`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `tailwind.config.js`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля `tailwind.config.js`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Частично (LLM-агент может написать код, но миграция схемы БД и apply terraform должны быть мануальными)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Настроить `content` пути строго по используемым директориям, чтобы purgeCSS корректно удалял неиспользуемые классы.

☐ Вес CSS бандла уменьшен.

---

### FE-079

- **Название**: Создание Theme Toggle (Dark/Light mode)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/components/ThemeToggle.tsx`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/frontend/components/ThemeToggle.tsx`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля ThemeToggle.tsx`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Использовать `next-themes` для бесшовного переключения тем (с поддержкой SSR без моргания).

☐ Смена темы сохраняется при перезагрузке страницы.

---

### FE-080

- **Название**: Анимации для пустых состояний (Empty States)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/app/history/page.tsx`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/frontend/app/history/page.tsx`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Низкий
- **Влияние**: Локальное улучшение модуля page.tsx`. Снижает технический долг в изолированной области.
- **Приоритет**: LOW
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Добавить красивый заглушечный компонент (иллюстрация + текст), если у пользователя нет истории генераций.

☐ Пустая история выглядит профессионально и направляет к действию.

---

### QA-081

- **Название**: Установка и конфигурация Jest
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/jest.config.js`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/backend-api/jest.config.js`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Настроить окружение тестирования для NestJS (ts-jest). Написать тестовый мок для глобальных логгеров.

☐ Jest запускается за секунды и находит файлы `.spec.ts`.

---

### QA-082

- **Название**: Создание мока БД (Prisma Mock)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/test/prisma.mock.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/backend-api/test/prisma.mock.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Установить `jest-mock-extended`. Написать фабрику `mockDeep<PrismaClient>()`.

☐ Unit-тесты не требуют реального подключения к PostgreSQL.

---

### QA-083

- **Название**: Unit-тест: AuthService (Регистрация)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/auth/auth.service.spec.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/backend-api/src/auth/auth.service.spec.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Написать тест на: 1) успешное создание юзера, 2) провал при дубликате email, 3) успешное хэширование пароля.

☐ `auth.service.ts` покрыт тестами на 90%.

---

### QA-084

- **Название**: Unit-тест: BillingService (Транзакции)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/billing/billing.service.spec.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/backend-api/src/billing/billing.service.spec.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Тест на атомарное списание кредитов (return false, если не хватает). Тест на начисление кредитов.

☐ `billing.service.ts` покрыт на 100%.

---

### QA-085

- **Название**: Unit-тест: RolesGuard
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/auth/guards/roles.guard.spec.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/backend-api/src/auth/guards/roles.guard.spec.ts`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля roles.guard.spec.ts`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Написать тесты на логику проверки массива ролей юзера из `ExecutionContext`.

☐ Guard гарантированно отбрасывает не те роли.

---

### QA-086

- **Название**: Установка и конфигурация Playwright
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `playwright.config.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `playwright.config.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Настроить Chromium/Webkit среды. Установить параметр `webServer` для запуска frontend на `localhost:3000` перед стартом тестов.

☐ Playwright корректно запускает изолированный браузер.

---

### QA-087

- **Название**: E2E тест: Страница Логина и Регистрации
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `tests/e2e/auth.spec.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `tests/e2e/auth.spec.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Прокликивание флоу регистрации (ввод email, пароля, submit). Проверка редиректа на страницу `/generate`.

☐ Автоматический браузер проходит регистрацию.

---

### QA-088

- **Название**: E2E тест: Флоу Генерации картинки
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `tests/e2e/generation.spec.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `tests/e2e/generation.spec.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 5h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Мокирование API запроса к бэкенду. Проверка, что после клика "Generate" появляется Loader, а затем картинка-результат.

☐ Критический путь покрыт стабильным тестом.

---

### QA-089

- **Название**: E2E тест: Проверка Admin Panel (Доступ запрещен)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `tests/e2e/admin-security.spec.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `tests/e2e/admin-security.spec.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля admin-security.spec.ts`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Авторизоваться как обычный юзер, попробовать перейти на `/admin`. Проверить, что возникает редирект или страница "Доступ запрещен".

☐ Тест доказывает безопасность админки на клиенте.

---

### QA-090

- **Название**: Интеграция Coverage-репортов
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `jest.config.js`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `jest.config.js`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля `jest.config.js`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Включить `collectCoverage: true`. Настроить `coverageThreshold` (минимум 70% на весь проект).

☐ Запуск тестов выдает красивую таблицу покрытия. Скрипт падает, если покрытие ниже 70%.

---

### DEV-091

- **Название**: Очистка корня проекта (Scripts)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `/` (Корень проекта).
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `/` (Корень проекта)
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля ` (Корень проекта). Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Убрать `send-report.js`, `test.py` и прочий мусор в папку `scripts/tools/`.

☐ В корне только файлы конфигурации (package.json, tsconfig и т.д.).

---

### DEV-092

- **Название**: Написание Multi-Stage Dockerfile для Backend
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/Dockerfile`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/backend-api/Dockerfile`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Написать Stage 1 (builder) и Stage 2 (runner). Устанавливать в runner только production-зависимости.

☐ Образ бэкенда занимает <250MB и не содержит компиляторов TypeScript.

---

### DEV-093

- **Название**: Написание Dockerfile для Worker
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/worker/Dockerfile`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/worker/Dockerfile`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Аналогичный билд процесс, но точка запуска `dist/main.js` указывает на воркер-приложение.

☐ Воркер запускается в изолированном контейнере.

---

### DEV-094

- **Название**: Оптимизация docker-compose.prod.yml
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `docker-compose.prod.yml`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `docker-compose.prod.yml`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Добавить сервисы `api`, `worker`, `postgres`, `redis`, `minio`. Настроить `depends_on` с использованием healthchecks.

☐ Поднятие стека происходит одной командой и без ошибок падений в первые секунды.

---

### DEV-095

- **Название**: Интеграция Sentry в Backend API
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/common/filters/sentry.filter.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/backend-api/src/common/filters/sentry.filter.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Установить `@sentry/node`. Подключить глобальный фильтр для отправки HTTP 500 исключений.

☐ Бросание `throw new Error('Test')` отображается в дашборде Sentry.

---

### DEV-096

- **Название**: Интеграция Sentry в Frontend (Next.js)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/sentry.client.config.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/frontend/sentry.client.config.ts`, `sentry.server.config.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Настроить официальный пакет `@sentry/nextjs`. Игнорировать мелкие ошибки (`ResizeObserver loop`).

☐ Ошибки браузера клиента автоматически прилетают разработчику.

---

### DEV-097

- **Название**: Интеграция Prometheus (Метрики)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/app.module.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/backend-api/src/app.module.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля app.module.ts`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Подключить `@willsoto/nestjs-prometheus`. Открыть эндпоинт `/metrics` (желательно защитить его базовой авторизацией).

☐ Метрики (CPU, RAM, HTTP запросы) экспортируются сервером.

---

### DEV-098

- **Название**: Создание PR-Check пайплайна (GitHub Actions)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `.github/workflows/pr.yml`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `.github/workflows/pr.yml`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Написать workflow для Pull Requests. Запускать: `pnpm lint`, `pnpm typecheck`, `pnpm test:cov`.

☐ GitHub блокирует слияние веток, если тесты или линтер упали.

---

### DEV-099

- **Название**: Создание Deploy пайплайна (GitHub Actions)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `.github/workflows/deploy.yml`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `.github/workflows/deploy.yml`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 4h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Настроить билд Docker-образов при пуше в `main` и деплой на сервер (через SSH или Render Deploy Hook).

☐ Изменения в коде автоматически (CD) доставляются на продакшен сервер после прохождения тестов.

---

### DEV-100

- **Название**: Обновление README.md проекта
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `README.md`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `README.md`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Низкий
- **Влияние**: Локальное улучшение модуля `README.md`. Снижает технический долг в изолированной области.
- **Приоритет**: LOW
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Написать актуальную инструкцию по локальному запуску всего стека (`pnpm install`, `docker-compose up db redis minio`, `pnpm start:dev`).

☐ Новый разработчик (или AI) может запустить проект локально за 10 минут, просто прочитав README.

---

### RT-101

- **Название**: Инициализация WebSocket Gateway в NestJS
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/events/events.gateway.ts`.
- **Почему это нужно**: Фронтенд использует HTTP-поллинг каждые 2 секунды для проверки статуса картинки, что убивает сервер.
- **Файлы**: `apps/backend-api/src/events/events.gateway.ts`
- **Зависимости**: ARC-002
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Установить `@nestjs/websockets` и `@nestjs/platform-socket.io`.

☐ Создать класс `EventsGateway` с декоратором `@WebSocketGateway({ cors: true })`.

☐ Настроить метод `handleConnection` для проверки JWT токена.

☐ Клиент может успешно установить `ws://` соединение с бэкендом, неавторизованные сокеты сбрасываются.

---

### RT-102

- **Название**: Настройка Redis Pub/Sub для WebSocket
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/adapters/redis-io.adapter.ts`.
- **Почему это нужно**: Если мы запустим несколько инстансов API сервера, сокеты не будут знать друг о друге (Client A подключен к Node 1, а событие пришло на Node 2).
- **Файлы**: `apps/backend-api/src/adapters/redis-io.adapter.ts`
- **Зависимости**: RT-101
- **Сложность**: High
- **Оценка времени**: 4h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Установить `@socket.io/redis-adapter` и `redis`.

☐ Создать кастомный `RedisIoAdapter`.

☐ В `main.ts` применить `app.useWebSocketAdapter(new RedisIoAdapter(app))`.

☐ Сообщения транслируются через Redis, обеспечивая горизонтальное масштабирование веб-сокетов.

---

### RT-103

- **Название**: Интеграция Воркера с Redis Pub/Sub
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/worker/src/processors/generation.processor.ts`.
- **Почему это нужно**: Воркер (в отдельном процессе) должен как-то сказать API-серверу, что картинка готова.
- **Файлы**: `apps/worker/src/processors/generation.processor.ts`
- **Зависимости**: RT-102
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ При статусе `COMPLETED` в BullMQ, воркер делает `redis.publish('socket-events', JSON.stringify({ userId, event: 'generation.done', payload }))`.

☐ Сообщения от воркера доходят до API сервера в реальном времени.

---

### RT-104

- **Название**: Маршрутизация событий конкретному пользователю
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/events/events.gateway.ts`.
- **Почему это нужно**: Одно событие не должно улетать всем подключенным клиентам (утечка приватных данных).
- **Файлы**: `apps/backend-api/src/events/events.gateway.ts`
- **Зависимости**: RT-103
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ При подключении делать `client.join(userId)`.

☐ При получении события из Redis делать `server.to(userId).emit('generation.done', payload)`.

☐ Пользователь получает уведомление только о своей сгенерированной картинке.

---

### RT-105

- **Название**: Интеграция Socket.io на Фронтенде
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/hooks/useSocket.ts`.
- **Почему это нужно**: На клиенте нет логики работы с WS.
- **Файлы**: `apps/frontend/hooks/useSocket.ts`
- **Зависимости**: RT-104
- **Сложность**: High
- **Оценка времени**: 4h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Установить клиентский `socket.io-client`.

☐ Написать хук `useSocket()`, который инициализирует соединение, прокидывая JWT из Cookies.

☐ Добавить слушатель: `socket.on('generation.done', (data) => queryClient.invalidateQueries('history'))`.

☐ Фронтенд автоматически обновляет UI при готовности картинки без поллинга.

---

### RT-106

- **Название**: Демонтаж старого HTTP-поллинга
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/components/generation/GenerationResult.tsx`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/frontend/components/generation/GenerationResult.tsx`
- **Зависимости**: RT-105
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля GenerationResult.tsx`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Удалить `setInterval` и рекурсивные вызовы `fetchStatus()` из компонентов. Очистить логику.

☐ Network Tab в браузере пуст во время ожидания генерации (нет HTTP спама).

---

### RT-107

- **Название**: Обработка статуса FAILED через WebSockets
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/worker/src/processors/generation.processor.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/worker/src/processors/generation.processor.ts`
- **Зависимости**: RT-103
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Если генерация падает с ошибкой, отправлять `redis.publish(..., { event: 'generation.failed' })`. На фронте показывать `toast.error('Сбой генерации, кредиты возвращены')`.

☐ Юзер мгновенно узнает о сбое, не ожидая бесконечно.

---

### RT-108

- **Название**: Индикатор прогресса генерации (Progress Bar)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/worker/src/providers/replicate.provider.ts`.
- **Почему это нужно**: Генерация занимает до 20 секунд, юзер не понимает, зависло ли всё.
- **Файлы**: `apps/worker/src/providers/replicate.provider.ts`
- **Зависимости**: RT-103
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля replicate.provider.ts`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Если API Replicate/OpenAI поддерживает webhook stream, транслировать прогресс (10%... 50%... 90%) в Redis Pub/Sub -> WebSocket. На фронте менять ширину Progress Bar.

☐ Пользователь видит плавную полосу загрузки.

---

### RT-109

- **Название**: Автоматический реконнект и оффлайн режим
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/hooks/useSocket.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/frontend/hooks/useSocket.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Низкий
- **Влияние**: Локальное улучшение модуля useSocket.ts`. Снижает технический долг в изолированной области.
- **Приоритет**: LOW
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Настроить логику `socket.io` на автоматическое восстановление при обрыве интернета. Если сокет упал, временно переключаться на редкий HTTP поллинг (Fallback).

☐ При плохом мобильном интернете приложение не зависает намертво.

---

### RT-110

- **Название**: Метрики WebSockets (Monitoring)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/events/events.gateway.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/backend-api/src/events/events.gateway.ts`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Низкий
- **Влияние**: Локальное улучшение модуля events.gateway.ts`. Снижает технический долг в изолированной области.
- **Приоритет**: LOW
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Экспортировать метрику `active_websocket_connections` в Prometheus.

☐ В Grafana видно график текущих одновременных WebSocket подключений (помогает при балансировке).

---

### RED-111

- **Название**: Внедрение CacheManager в NestJS
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/app.module.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/backend-api/src/app.module.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Установить `@nestjs/cache-manager` и `cache-manager-redis-store`. Подключить в глобальный модуль.

☐ Сервис `CacheManager` доступен для инъекции через `@Inject(CACHE_MANAGER)` в любом контроллере.

---

### RED-112

- **Название**: Кэширование списка шаблонов (Marketplace)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/templates/templates.controller.ts`.
- **Почему это нужно**: Каждый запрос к маркетплейсу дергает PostgreSQL.
- **Файлы**: `apps/backend-api/src/templates/templates.controller.ts`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Добавить `@UseInterceptors(CacheInterceptor)` и `@CacheKey('templates_all')`, `@CacheTTL(3600)` на метод `findAll()`.

☐ Ответы API по шаблонам отдаются из Redis за <5ms, полностью разгружая БД.

---

### RED-113

- **Название**: Инвалидация кэша шаблонов при изменениях
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/admin/admin-templates.controller.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/backend-api/src/admin/admin-templates.controller.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля admin-templates.controller.ts`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ При создании, удалении или редактировании шаблона администратором, вызывать `cacheManager.del('templates_all')`.

☐ Изменения в админке мгновенно появляются на маркетплейсе без ожидания часа.

---

### RED-114

- **Название**: Кэширование профиля пользователя (User Profile)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/users/users.service.ts`.
- **Почему это нужно**: Метод `GET /auth/me` вызывается при каждом F5 на клиенте.
- **Файлы**: `apps/backend-api/src/users/users.service.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля users.service.ts`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Кэшировать объект профиля юзера по ключу `user_profile:${userId}` на 5 минут. При изменении баланса кредитов (Billing) инвалидировать кэш `cacheManager.del()`.

☐ Существенное снижение нагрузки на БД при высоком онлайне.

---

### RED-115

- **Название**: Настройка Redis Eviction Policy
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `docker-compose.prod.yml`.
- **Почему это нужно**: Redis может упасть от нехватки RAM (Out of Memory).
- **Файлы**: `docker-compose.prod.yml`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Задать флаг при запуске Redis контейнера: `--maxmemory 512mb --maxmemory-policy allkeys-lru`.

☐ Redis автоматически удаляет старые кэши, если память заканчивается.

---

### RED-116

- **Название**: Идемпотентность транзакций оплаты (Redis Locks)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/billing/billing.service.ts`.
- **Почему это нужно**: Юзер может кликнуть "Оплатить" 10 раз за секунду, вызвав Race Condition при списании/начислении кредитов.
- **Файлы**: `apps/backend-api/src/billing/billing.service.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 4h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Внедрить пакет `redlock` (Redis-based distributed locks). Блокировать мьютекс по ключу `lock:billing:${userId}` на 2 секунды перед транзакцией.

☐ Невозможно списать баланс дважды при параллельных HTTP-запросах.

---

### RED-117

- **Название**: Кэширование DNS и сторонних API ответов
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/providers/stripe.service.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/backend-api/src/providers/stripe.service.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Низкий
- **Влияние**: Локальное улучшение модуля stripe.service.ts`. Снижает технический долг в изолированной области.
- **Приоритет**: LOW
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Если обращаемся к внешнему API для получения статичных цен (Stripe Prices), кэшировать их в Redis на 24 часа.

☐ Ускорение чекаута.

---

### RED-118

- **Название**: Оптимизация сессий BullMQ (Queue UI)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/admin/bull-board.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/backend-api/src/admin/bull-board.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля bull-board.ts`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Подключить `@bull-board/nestjs`. Вывести UI очередей на `/admin/queues` под защитой Basic Auth или Admin Guard.

☐ Администратор визуально видит зависшие задачи, количество упавших джобов и может перезапустить их кнопкой "Retry".

---

### RED-119

- **Название**: Отдельные БД для Cache и BullMQ
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `.env`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `.env`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Низкий
- **Влияние**: Локальное улучшение модуля `.env`. Снижает технический долг в изолированной области.
- **Приоритет**: LOW
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Настроить `REDIS_CACHE_URL=redis://localhost:6379/1` и `REDIS_QUEUE_URL=redis://localhost:6379/2`.

☐ Очистка кэша командой `FLUSHDB` (база 1) не убивает задачи в очереди (база 2).

---

### RED-120

- **Название**: Мониторинг RAM потребления Redis
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `docker-compose.prod.yml`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `docker-compose.prod.yml`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля `docker-compose.prod.yml`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Добавить `redis_exporter` для выгрузки метрик в Prometheus.

☐ В Grafana настроен алерт (Alert), если Redis упирается в 90% доступной памяти.

---

### DB-121

- **Название**: Внедрение Prisma Extensions (Soft Deletes)
- **Описание**: Агрессивная миграция схемы данных и оптимизация слоя персистентности (Prisma ORM). Разгрузка PostgreSQL и пулеров коннектов.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `packages/database/src/client.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Любую миграцию схемы выполнять строго в два этапа (Expand and Contract паттерн), чтобы полностью исключить блокировки таблицы (Table Locks) и обеспечить Zero-Downtime.

**Критерии завершения (DoD)**:
☐ Использовать новый синтаксис Prisma `prisma.$extends()`. Перехватывать запросы `delete` и `deleteMany`, заменяя их на `update({ deletedAt: new Date() })`.

☐ Любой разработчик вызывает `.delete()`, но физически данные не удаляются.

---

### DB-122

- **Название**: Prisma Extensions: Автоматическое скрытие Soft-Deleted записей
- **Описание**: Агрессивная миграция схемы данных и оптимизация слоя персистентности (Prisma ORM). Разгрузка PostgreSQL и пулеров коннектов.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `packages/database/src/client.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Любую миграцию схемы выполнять строго в два этапа (Expand and Contract паттерн), чтобы полностью исключить блокировки таблицы (Table Locks) и обеспечить Zero-Downtime.

**Критерии завершения (DoD)**:
☐ В том же Extension перехватывать методы `findMany`, `findUnique`, `findFirst`, автоматически добавляя `where: { deletedAt: null }`.

☐ Не нужно переписывать весь бэкенд. Удаленные записи магически "исчезают" для всего приложения.

---

### DB-123

- **Название**: Таблица Audit Logs (Журнал действий)
- **Описание**: Агрессивная миграция схемы данных и оптимизация слоя персистентности (Prisma ORM). Разгрузка PostgreSQL и пулеров коннектов.
- **Почему это нужно**: Если кто-то удалил шаблон или выдал юзеру 1000 кредитов, невозможно найти крайнего.
- **Файлы**: `packages/database/prisma/schema.prisma`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля schema.prisma`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Любую миграцию схемы выполнять строго в два этапа (Expand and Contract паттерн), чтобы полностью исключить блокировки таблицы (Table Locks) и обеспечить Zero-Downtime.

**Критерии завершения (DoD)**:
☐ Создать таблицу `AuditLog (id, action, userId, targetId, oldData, newData, createdAt)`.

☐ В Prisma Middleware (или Interceptor в Nest) писать лог при всех мутациях в админке.

☐ Полная прозрачность действий администраторов и системы.

---

### DB-124

- **Название**: Партицирование таблицы Generations (Partitioning)
- **Описание**: Агрессивная миграция схемы данных и оптимизация слоя персистентности (Prisma ORM). Разгрузка PostgreSQL и пулеров коннектов.
- **Почему это нужно**: Таблица `Generation` растет на миллионы строк в месяц. Скоро индексы перестанут влезать в RAM PostgreSQL.
- **Файлы**: `packages/database/prisma/migrations/`
- **Зависимости**: Нет
- **Сложность**: Extreme (Требует пары сеньор-разработчиков)
- **Оценка времени**: 6h
- **Риск**: Низкий
- **Влияние**: Локальное улучшение модуля `. Снижает технический долг в изолированной области.
- **Приоритет**: LOW
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Любую миграцию схемы выполнять строго в два этапа (Expand and Contract паттерн), чтобы полностью исключить блокировки таблицы (Table Locks) и обеспечить Zero-Downtime.

**Критерии завершения (DoD)**:
☐ Написать raw SQL миграцию для партицирования таблицы по месяцам (`PARTITION BY RANGE (createdAt)`).

☐ БД легко переваривает миллионы строк, архивирование старых данных сводится к `DROP PARTITION`.

---

### DB-125

- **Название**: Вынос тяжелых JSON в отдельные таблицы
- **Описание**: Агрессивная миграция схемы данных и оптимизация слоя персистентности (Prisma ORM). Разгрузка PostgreSQL и пулеров коннектов.
- **Почему это нужно**: Колонка `Generation.metadata` (хранящая весь JSON ответ от OpenAI) делает таблицу слишком "широкой" и замедляет выборки ленты (Feed).
- **Файлы**: `packages/database/prisma/schema.prisma`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 4h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля schema.prisma`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Любую миграцию схемы выполнять строго в два этапа (Expand and Contract паттерн), чтобы полностью исключить блокировки таблицы (Table Locks) и обеспечить Zero-Downtime.

**Критерии завершения (DoD)**:
☐ Вынести JSON в таблицу `GenerationMetadata` со связью 1-to-1.

☐ Запросы на получение списков (без JOIN'а метаданных) отрабатывают в 2 раза быстрее.

---

### DB-126

- **Название**: Database Seeder для тестов (Factory Pattern)
- **Описание**: Агрессивная миграция схемы данных и оптимизация слоя персистентности (Prisma ORM). Разгрузка PostgreSQL и пулеров коннектов.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `packages/database/test/factories.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Любую миграцию схемы выполнять строго в два этапа (Expand and Contract паттерн), чтобы полностью исключить блокировки таблицы (Table Locks) и обеспечить Zero-Downtime.

**Критерии завершения (DoD)**:
☐ Создать утилиты для быстрой генерации mock-данных. Использовать пакет `faker.js`. Функция `UserFactory.create({ credits: 500 })`.

☐ Написание E2E тестов и наполнение локальной БД сводится к 2-м строкам кода.

---

### DB-127

- **Название**: Настройка PgBouncer (Connection Pooling)
- **Описание**: Агрессивная миграция схемы данных и оптимизация слоя персистентности (Prisma ORM). Разгрузка PostgreSQL и пулеров коннектов.
- **Почему это нужно**: NestJS Prisma создает много коннектов к БД. При 10 воркерах и 5 API подах мы упремся в лимит `max_connections` (обычно 100).
- **Файлы**: `docker-compose.prod.yml`, `.env`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Любую миграцию схемы выполнять строго в два этапа (Expand and Contract паттерн), чтобы полностью исключить блокировки таблицы (Table Locks) и обеспечить Zero-Downtime.

**Критерии завершения (DoD)**:
☐ Добавить контейнер PgBouncer. Настроить Prisma на `postgresql://pgbouncer-url?pgbouncer=true`.

☐ Количество прямых соединений к Postgres никогда не превышает 20, а PgBouncer держит тысячи клиентских коннектов.

---

### DB-128

- **Название**: Настройка Prisma Accelerate / Edge (Опционально)
- **Описание**: Агрессивная миграция схемы данных и оптимизация слоя персистентности (Prisma ORM). Разгрузка PostgreSQL и пулеров коннектов.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `packages/database/src/client.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Низкий
- **Влияние**: Локальное улучшение модуля client.ts`. Снижает технический долг в изолированной области.
- **Приоритет**: LOW
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Любую миграцию схемы выполнять строго в два этапа (Expand and Contract паттерн), чтобы полностью исключить блокировки таблицы (Table Locks) и обеспечить Zero-Downtime.

**Критерии завершения (DoD)**:
☐ Если Vercel Edge Functions будут использоваться для фронтенда, подключить `@prisma/extension-accelerate`.

☐ Возможность кэшировать запросы БД прямо на глобальных Edge-узлах.

---

### DB-129

- **Название**: Перевод UUIDv4 на UUIDv7 (или CUID2)
- **Описание**: Агрессивная миграция схемы данных и оптимизация слоя персистентности (Prisma ORM). Разгрузка PostgreSQL и пулеров коннектов.
- **Почему это нужно**: Стандартные UUIDv4 не отсортированы по времени, что вызывает фрагментацию индексов (B-Tree) при вставках.
- **Файлы**: `packages/database/prisma/schema.prisma`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 4h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля schema.prisma`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Любую миграцию схемы выполнять строго в два этапа (Expand and Contract паттерн), чтобы полностью исключить блокировки таблицы (Table Locks) и обеспечить Zero-Downtime.

**Критерии завершения (DoD)**:
☐ Заменить `@default(uuid())` на `@default(cuid())` в Prisma (CUID2) во всех новых таблицах.

☐ Скорость вставки `INSERT` не деградирует со временем по мере роста таблицы.

---

### DB-130

- **Название**: Индекс для Full-Text Search
- **Описание**: Агрессивная миграция схемы данных и оптимизация слоя персистентности (Prisma ORM). Разгрузка PostgreSQL и пулеров коннектов.
- **Почему это нужно**: Поиск шаблонов через `ILIKE '%...%'` не масштабируется.
- **Файлы**: `packages/database/prisma/schema.prisma`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля schema.prisma`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Любую миграцию схемы выполнять строго в два этапа (Expand and Contract паттерн), чтобы полностью исключить блокировки таблицы (Table Locks) и обеспечить Zero-Downtime.

**Критерии завершения (DoD)**:
☐ Добавить PostgreSQL GIN индекс для `Template.name` и `Template.description`. Использовать `search` API в Prisma.

☐ Поиск по сотням тысяч шаблонов выполняется за миллисекунды.

---

### API-131

- **Название**: Внедрение версионирования в NestJS (URI Versioning)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/main.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/backend-api/src/main.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Включить `app.enableVersioning({ type: VersioningType.URI, defaultVersion: '1' })`.

☐ Все эндпоинты по умолчанию доступны по префиксу `/api/v1/...`.

---

### API-132

- **Название**: Отвязка авторизации от HttpOnly Cookie (Для Мобилок)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/auth/jwt.strategy.ts`.
- **Почему это нужно**: HttpOnly Cookies (внедренные в SEC-005) отлично работают в браузере, но iOS/Android приложениям проще передавать `Authorization: Bearer <token>`.
- **Файлы**: `apps/backend-api/src/auth/jwt.strategy.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 4h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Изменить JWT Strategy так, чтобы она искала токен ИЛИ в Cookie `access_token`, ИЛИ в заголовке `Authorization`.

☐ Мобильные приложения (Flutter/React Native) могут использовать наше API без боли с Cookie-менеджерами.

---

### API-133

- **Название**: Отвязка от Google OAuth Redirect (Для Мобилок)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/auth/auth.controller.ts`.
- **Почему это нужно**: Мобилки делают Google Sign-In на своей стороне через нативный SDK и получают `id_token`. Наш бэкенд этого не понимает.
- **Файлы**: `apps/backend-api/src/auth/auth.controller.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 4h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Сделать эндпоинт `POST /api/v1/auth/google/verify-token`, который принимает `id_token` от мобилки, валидирует его через `google-auth-library` и выдает наш JWT.

☐ Мобильные разработчики могут авторизовать юзера в одно действие.

---

### API-134

- **Название**: Pagination Strategy: Переход от Offset к Cursor (Keyset) Pagination
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/generations/generations.controller.ts`.
- **Почему это нужно**: Пагинация `skip: 50000, take: 20` в PostgreSQL очень медленная (Offset Scanning).
- **Файлы**: `apps/backend-api/src/generations/generations.controller.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 5h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля generations.controller.ts`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Заменить API получения Feed'а. Вместо `page` и `limit`, принимать `cursor` (ID последней записи) и использовать `take`.

☐ Бесконечная лента (Infinite Scroll) в мобильном приложении работает быстро и без дублей, даже если во время скролла появляются новые картинки.

---

### API-135

- **Название**: Адаптация DTO под строгие проверки мобильных платформ
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/**/*.dto.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/backend-api/src/**/*.dto.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Использовать `@Transform()` из `class-transformer` для обработки boolean полей (мобилки иногда шлют `?isPublic=true` как строку, а Nest ожидает boolean).

☐ Отсутствуют ошибки "isPublic must be a boolean" из-за особенностей Android/iOS HTTP-клиентов.

---

### API-136

- **Название**: Создание эндпоинта проверки версии (Force Update)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/system/system.controller.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/backend-api/src/system/system.controller.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Реализовать `GET /api/v1/system/config`. Возвращать `{ minClientVersion: "1.2.0", maintenanceMode: false }`.

☐ Мобильное приложение знает, когда нужно заблокировать юзера и попросить обновиться в AppStore.

---

### API-137

- **Название**: Поддержка Push Уведомлений (FCM/APNS)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/worker/src/notifications/`.
- **Почему это нужно**: WebSockets закрываются, когда мобильное приложение уходит в фон. Юзер не узнает, что картинка сгенерировалась.
- **Файлы**: `apps/worker/src/notifications/`
- **Зависимости**: Нет
- **Сложность**: Extreme (Требует пары сеньор-разработчиков)
- **Оценка времени**: 6h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля `. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Добавить таблицу `DeviceToken` в БД.

☐ Привязать Firebase Admin SDK в воркер.

☐ Отправлять Push, если задача занимала больше 1 минуты.

☐ Юзер получает уведомление "Твоя картинка готова!" прямо на экран смартфона.

---

### API-138

- **Название**: Настройка Compression (Gzip/Brotli)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/main.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/backend-api/src/main.ts`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля main.ts`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Использовать пакет `compression` в NestJS.

☐ Ответы с массивами JSON (списки шаблонов) сжимаются в 5-10 раз, экономя трафик пользователям мобильного интернета.

---

### API-139

- **Название**: Глубокие ссылки (Deep Links / Universal Links)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/app/.well-known/apple-app-site-association`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/frontend/app/.well-known/apple-app-site-association`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Низкий
- **Влияние**: Локальное улучшение модуля apple-app-site-association`. Снижает технический долг в изолированной области.
- **Приоритет**: LOW
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Настроить AASA файл на фронтенде, чтобы ссылки вида `arrena.ai/g/123` автоматически открывали iOS приложение, если оно установлено.

☐ Бесшовная интеграция веба и мобилки.

---

### API-140

- **Название**: Оффлайн режим (Sync Queue) на клиенте
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/lib/offlineSync.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/frontend/lib/offlineSync.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 5h
- **Риск**: Низкий
- **Влияние**: Локальное улучшение модуля offlineSync.ts`. Снижает технический долг в изолированной области.
- **Приоритет**: LOW
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Если юзер кликает лайк или удаляет картинку без сети, сохранять действие в IndexedDB. При появлении интернета — делать фоновый sync с бэкендом.

☐ Приложение ощущается молниеносным ("Optimistic UI"), не блокируется лоадерами.

---

### SEO-151

- **Название**: Настройка Edge Caching (Vercel/Cloudflare)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/app/marketplace/page.tsx`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/frontend/app/marketplace/page.tsx`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Использовать Next.js `revalidate` API. Сконфигурировать `export const revalidate = 3600;` на публичных страницах.

☐ Страницы кэшируются на CDN Edge. TTFB (Time To First Byte) падает до <50ms для посетителей из любых стран.

---

### SEO-152

- **Название**: On-Demand Revalidation
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/app/api/revalidate/route.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/frontend/app/api/revalidate/route.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля route.ts`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Написать API эндпоинт во фронтенде. При добавлении шаблона админом, бэкенд дергает этот вебхук, чтобы моментально сбросить кэш конкретной страницы: `revalidatePath('/marketplace')`.

☐ Кэш вечный (быстрый), но обновляется мгновенно при реальных изменениях.

---

### SEO-153

- **Название**: Внедрение JSON-LD Schema (Structured Data)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/app/marketplace/[slug]/page.tsx`.
- **Почему это нужно**: Google не понимает контекст (что это AI шаблон или Изображение).
- **Файлы**: `apps/frontend/app/marketplace/[slug]/page.tsx`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Встроить `<script type="application/ld+json">` с разметкой схемы `SoftwareApplication` или `ImageObject`. Включить рейтинг, цену (Free) и автора.

☐ В Google Search появляются Rich Snippets (красивые карточки с рейтингом прямо в выдаче).

---

### SEO-154

- **Название**: Оптимизация Largest Contentful Paint (LCP)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/app/page.tsx`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/frontend/app/page.tsx`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Главный Hero-баннер или главная картинка должны загружаться приоритетно. Добавить `priority={true}` в `next/image` для первого экрана. Использовать `fetchPriority="high"`.

☐ Lighthouse показывает LCP < 2.5 сек.

---

### SEO-155

- **Название**: Устранение Layout Shift (CLS) в Сетке шаблонов
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/components/marketplace/TemplateGrid.tsx`.
- **Почему это нужно**: Картинки в masonry-сетке загружаются прыжками, ломая верстку.
- **Файлы**: `apps/frontend/components/marketplace/TemplateGrid.tsx`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Задавать жесткий `aspect-ratio` или фиксированные `width/height` для скелетонов (placeholders), которые в точности совпадают с размерами финальной картинки.

☐ Страница загружается монолитно, элементы не сдвигаются.

---

### PERF-156

- **Название**: Подключение Bundle Analyzer
- **Описание**: Экстремальная архитектурная оптимизация подсистемы вычислений. Кардинальное снижение нагрузки на Event Loop и дисковый I/O.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/frontend/next.config.js`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля next.config.js`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Установить `@next/bundle-analyzer`. Найти и удалить огромные библиотеки (например, `moment.js` заменить на `date-fns` или нативный API).

☐ Размер клиентского JS бандла уменьшен минимум на 20%.

---

### PERF-157

- **Название**: Ленивая загрузка тяжелых компонентов (Lazy Loading)
- **Описание**: Экстремальная архитектурная оптимизация подсистемы вычислений. Кардинальное снижение нагрузки на Event Loop и дисковый I/O.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/frontend/app/generate/page.tsx`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Использовать `next/dynamic` для импорта тяжелых модалок, тултипов или редакторов (например, React-Cropper для обрезки аватара).

☐ Эти компоненты грузятся по сети только в момент клика юзером.

---

### PERF-158

- **Название**: Оптимизация шрифтов (Preloading)
- **Описание**: Экстремальная архитектурная оптимизация подсистемы вычислений. Кардинальное снижение нагрузки на Event Loop и дисковый I/O.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/frontend/app/layout.tsx`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля layout.tsx`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Убедиться, что критичные шрифты имеют `<link rel="preload">`. `next/font` делает это автоматически, нужно проверить конфигурацию (subsets).

☐ Нет блокировки рендера из-за загрузки .woff2 файлов.

---

### PERF-159

- **Название**: Дедупликация API запросов на сервере
- **Описание**: Экстремальная архитектурная оптимизация подсистемы вычислений. Кардинальное снижение нагрузки на Event Loop и дисковый I/O.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/frontend/lib/api.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Низкий
- **Влияние**: Локальное улучшение модуля api.ts`. Снижает технический долг в изолированной области.
- **Приоритет**: LOW
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Использовать React `cache()` для обертки запросов, если несколько серверных компонентов на одной странице делают одинаковый `fetch('/profile')`.

☐ На сервер бэкенда уходит только 1 HTTP запрос, даже если компонент вызван 5 раз.

---

### PERF-160

- **Название**: Настройка Service Worker (PWA)
- **Описание**: Экстремальная архитектурная оптимизация подсистемы вычислений. Кардинальное снижение нагрузки на Event Loop и дисковый I/O.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/frontend/next.config.js`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 4h
- **Риск**: Низкий
- **Влияние**: Локальное улучшение модуля next.config.js`. Снижает технический долг в изолированной области.
- **Приоритет**: LOW
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Использовать `next-pwa`. Кэшировать статику и шрифты для работы в Offline. Добавить `manifest.json`.

☐ Приложение можно установить на домашний экран смартфона, оно открывается мгновенно как нативное (без панели браузера).

---

### UI-161

- **Название**: Внедрение Framer Motion (Анимации)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/components/ui/`.
- **Почему это нужно**: Интерфейс резкий, появление элементов выглядит "дешево".
- **Файлы**: `apps/frontend/components/ui/`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля `. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Обернуть модальные окна, dropdown-меню и тосты в `<motion.div>` с анимациями `opacity` и `y` (сдвиг).

☐ UI ощущается премиальным и плавным (Wow-эффект).

---

### UI-162

- **Название**: Плавные переходы страниц (Page Transitions)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/app/template.tsx` (App Router feature).
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/frontend/app/template.tsx` (App Router feature)
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля template.tsx` (App Router feature). Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Использовать файл `template.tsx` в сочетании с Framer Motion для создания Fade-in/Fade-out анимации при навигации между роутами.

☐ Страницы не "прыгают", а плавно перетекают друг в друга.

---

### UI-163

- **Название**: Shimmer Effect для Loading States
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/components/ui/Skeleton.tsx`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/frontend/components/ui/Skeleton.tsx`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Реализовать пульсирующие заглушки (Shimmer Skeletons) для формы генерации, истории и маркетплейса, которые появляются до загрузки данных.

☐ Вместо крутящегося спиннера юзер видит контуры будущего контента.

---

### UI-164

- **Название**: Интерактивный Hover на карточках (Magic Glow)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/components/marketplace/TemplateCard.tsx`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/frontend/components/marketplace/TemplateCard.tsx`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Низкий
- **Влияние**: Локальное улучшение модуля TemplateCard.tsx`. Снижает технический долг в изолированной области.
- **Приоритет**: LOW
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Добавить CSS градиент, который следит за курсором мыши (mouse tracking glow) на карточках генераций.

☐ Дизайн соответствует топ-уровню (как на сайте Vercel или Linear).

---

### UI-165

- **Название**: Toast Notification Manager (Sonner)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/app/layout.tsx`.
- **Почему это нужно**: Дефолтные аллерты (или старые тосты) перекрывают UI и выглядят плохо.
- **Файлы**: `apps/frontend/app/layout.tsx`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Установить библиотеку `sonner`. Заменить все вызовы `alert()` и старых нотификаций.

☐ Красивые стекующиеся (stackable) тосты в углу экрана.

---

### UI-166

- **Название**: Оптимистичные обновления UI (Optimistic Updates)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/hooks/useLike.ts`.
- **Почему это нужно**: При лайке картинки юзер ждет 1 сек ответа от сервера, прежде чем сердечко закрасится.
- **Файлы**: `apps/frontend/hooks/useLike.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 4h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Использовать функцию `onMutate` в React Query или `useOptimistic` в React 19. При клике МГНОВЕННО закрашивать сердечко, а если сервер вернул ошибку — откатывать состояние назад.

☐ Приложение ощущается мгновенным (Zero Latency Experience).

---

### UI-167

- **Название**: Виртуализация длинных списков (Windowing)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/components/history/GenerationList.tsx`.
- **Почему это нужно**: У активного юзера 500+ картинок в истории. Рендер 500 узлов DOM убивает браузер.
- **Файлы**: `apps/frontend/components/history/GenerationList.tsx`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Использовать пакет `@tanstack/react-virtual` (или `react-window`). Рендерить только те 10 карточек, которые сейчас видны на экране.

☐ Скролл 1000 картинок держит стабильные 60 FPS на слабом смартфоне.

---

### UI-168

- **Название**: Защита от потери введенных данных (Unsaved Changes)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/components/generate/PromptForm.tsx`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/frontend/components/generate/PromptForm.tsx`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля PromptForm.tsx`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Если юзер ввел длинный промпт (Prompt) и случайно нажал кнопку "Назад" в браузере или обновил страницу, показывать `window.confirm('Вы не сохранили данные, точно уйти?')`.

☐ Предотвращение потери контекста и фрустрации пользователя.

---

### UI-169

- **Название**: Контекстное меню (Right Click Menu)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/components/ui/ContextMenu.tsx`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/frontend/components/ui/ContextMenu.tsx`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Низкий
- **Влияние**: Локальное улучшение модуля ContextMenu.tsx`. Снижает технический долг в изолированной области.
- **Приоритет**: LOW
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Заменить стандартное меню правой кнопки мыши на сгенерированных картинках на кастомное UI меню ("Скачать HD", "Удалить", "Скопировать Промпт", "Сделать приватным").

☐ Нативный опыт (App-like feel).

---

### UI-170

- **Название**: Поддержка Drag and Drop
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/frontend/components/generate/ImageToImage.tsx`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/frontend/components/generate/ImageToImage.tsx`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля ImageToImage.tsx`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Реализовать зону (Dropzone) для перетаскивания исходных картинок (для режима Image-to-Image генерации) прямо с рабочего стола юзера.

☐ Удобный UX без необходимости кликать "Choose file".

---

### SEC-171

- **Название**: Внедрение Multi-Factor Authentication (MFA / 2FA)
- **Описание**: Критическое обновление периметра безопасности. Немедленное устранение вектора атаки, связанного с внедрение multi-factor authentication (mfa / 2fa).
- **Почему это нужно**: Компрометация пароля приведет к потере аккаунта.
- **Файлы**: `apps/backend-api/src/auth/mfa.service.ts`
- **Зависимости**: Нет
- **Сложность**: Extreme (Требует пары сеньор-разработчиков)
- **Оценка времени**: 6h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля mfa.service.ts`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Использовать криптостойкие алгоритмы. Категорически запрещено выводить детали ошибки (Stack Traces) в HTTP ответ.

**Критерии завершения (DoD)**:
☐ Реализовать генерацию TOTP секретов (`otplib`).

☐ Возвращать QR-код (URL `otpauth://`) для Google Authenticator.

☐ Требовать `/verify-totp` при логине, если у юзера `isMfaEnabled == true`.

☐ Защита аккаунта на банковском уровне.

---

### SEC-172

- **Название**: Поддержка WebAuthn (Passkeys / FaceID) - Опционально
- **Описание**: Критическое обновление периметра безопасности. Немедленное устранение вектора атаки, связанного с поддержка webauthn (passkeys / faceid) - опционально.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/backend-api/src/auth/webauthn.service.ts`
- **Зависимости**: Нет
- **Сложность**: Extreme (Требует пары сеньор-разработчиков)
- **Оценка времени**: 8h
- **Риск**: Низкий
- **Влияние**: Локальное улучшение модуля webauthn.service.ts`. Снижает технический долг в изолированной области.
- **Приоритет**: LOW
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Использовать криптостойкие алгоритмы. Категорически запрещено выводить детали ошибки (Stack Traces) в HTTP ответ.

**Критерии завершения (DoD)**:
☐ Внедрить пакет `@simplewebauthn`. Позволить логиниться через отпечаток пальца (TouchID) или FaceID на смартфонах без паролей.

☐ Логин в один клик без ввода паролей (Passwordless experience).

---

### SEC-173

- **Название**: Модуль Audit & Anomaly Detection (Fraud Guard)
- **Описание**: Критическое обновление периметра безопасности. Немедленное устранение вектора атаки, связанного с модуль audit & anomaly detection (fraud guard).
- **Почему это нужно**: Регистрация 100 аккаунтов для получения Free кредитов (Abuse).
- **Файлы**: `apps/backend-api/src/security/fraud.service.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 4h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Использовать криптостойкие алгоритмы. Категорически запрещено выводить детали ошибки (Stack Traces) в HTTP ответ.

**Критерии завершения (DoD)**:
☐ Логировать IP адреса при регистрации.

☐ Блокировать регистрацию, если с одного IP/Subnet создано >5 аккаунтов за день.

☐ Автоматическая защита от создания ферм (Sybil attacks).

---

### SEC-174

- **Название**: Шифрование параметров (Cursor / Pagination)
- **Описание**: Критическое обновление периметра безопасности. Немедленное устранение вектора атаки, связанного с шифрование параметров (cursor / pagination).
- **Почему это нужно**: Юзер видит ID в курсоре `?cursor=UUID` и может угадывать данные.
- **Файлы**: `apps/backend-api/src/common/utils/cursor.util.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля cursor.util.ts`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Использовать криптостойкие алгоритмы. Категорически запрещено выводить детали ошибки (Stack Traces) в HTTP ответ.

**Критерии завершения (DoD)**:
☐ Кодировать cursor payload в Base64 с простой солью (чтобы сделать его Opaque Token).

☐ Курсор выглядит как `opaque_v1_xyz...` и не раскрывает внутренние ID базы.

---

### SEC-175

- **Название**: Strict Transport Security (HSTS) и Cookies Secure Flag
- **Описание**: Критическое обновление периметра безопасности. Немедленное устранение вектора атаки, связанного с strict transport security (hsts) и cookies secure flag.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/backend-api/src/main.ts`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Использовать криптостойкие алгоритмы. Категорически запрещено выводить детали ошибки (Stack Traces) в HTTP ответ.

**Критерии завершения (DoD)**:
☐ Убедиться, что `Strict-Transport-Security: max-age=31536000; includeSubDomains` включен. Куки выдаются с `Secure: true` (работают только по HTTPS).

☐ Атака Man-in-the-Middle (Downgrade to HTTP) невозможна.

---

### SEC-176

- **Название**: Защита от перебора OTP-кодов (Email Verification)
- **Описание**: Критическое обновление периметра безопасности. Немедленное устранение вектора атаки, связанного с защита от перебора otp-кодов (email verification).
- **Почему это нужно**: 6-значный код верификации почты можно подобрать брутфорсом (10^6 комбинаций).
- **Файлы**: `apps/backend-api/src/auth/auth.service.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Использовать криптостойкие алгоритмы. Категорически запрещено выводить детали ошибки (Stack Traces) в HTTP ответ.

**Критерии завершения (DoD)**:
☐ В Redis хранить количество попыток. Максимум 5 попыток, после чего код инвалидируется.

☐ Брутфорс 6-значного кода статистически невозможен.

---

### SEC-177

- **Название**: Усиление Password Policy
- **Описание**: Критическое обновление периметра безопасности. Немедленное устранение вектора атаки, связанного с усиление password policy.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/backend-api/src/auth/dto/register.dto.ts`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Использовать криптостойкие алгоритмы. Категорически запрещено выводить детали ошибки (Stack Traces) в HTTP ответ.

**Критерии завершения (DoD)**:
☐ Настроить валидатор: минимум 8 символов, 1 заглавная буква, 1 цифра, 1 спецсимвол. Использовать `zxcvbn` библиотеку для отклонения популярных паролей ("qwerty", "password123").

☐ Юзер не может создать уязвимый аккаунт.

---

### SEC-178

- **Название**: Content Security Policy (CSP) для Frontend
- **Описание**: Критическое обновление периметра безопасности. Немедленное устранение вектора атаки, связанного с content security policy (csp) для frontend.
- **Почему это нужно**: Сайт уязвим к Cross-Site Scripting (XSS), если злоумышленник внедрит `<script src="hacker.com/steal.js">`.
- **Файлы**: `apps/frontend/next.config.js`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля next.config.js`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Использовать криптостойкие алгоритмы. Категорически запрещено выводить детали ошибки (Stack Traces) в HTTP ответ.

**Критерии завершения (DoD)**:
☐ Настроить CSP заголовки. Разрешить загрузку скриптов, картинок и коннектов (`connect-src`) ТОЛЬКО с наших доверенных доменов (S3, API, Google Analytics).

☐ Внедренный чужой JS скрипт просто блокируется браузером.

---

### SEC-179

- **Название**: Удаление Stack Traces в Production
- **Описание**: Критическое обновление периметра безопасности. Немедленное устранение вектора атаки, связанного с удаление stack traces в production.
- **Почему это нужно**: Иногда NestJS 500 ошибки могут сливать пути на сервере (`/app/src/...`).
- **Файлы**: `apps/backend-api/src/common/filters/global.filter.ts`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Частично (LLM-агент может написать код, но миграция схемы БД и apply terraform должны быть мануальными)
- **Рекомендации по реализации**: Использовать криптостойкие алгоритмы. Категорически запрещено выводить детали ошибки (Stack Traces) в HTTP ответ.

**Критерии завершения (DoD)**:
☐ В Exception Filter делать жесткий `return { statusCode: 500, message: 'Internal Server Error' }` для `NODE_ENV === 'production'`, без деталей. Детали уходят только в Sentry.

☐ Сервер молчит о своей внутренней структуре при падениях.

---

### SEC-180

- **Название**: Аудит Stripe Webhooks
- **Описание**: Критическое обновление периметра безопасности. Немедленное устранение вектора атаки, связанного с аудит stripe webhooks.
- **Почему это нужно**: Хакер может послать POST запрос на `/webhooks/stripe` и "симулировать" успешную оплату, бесплатно пополнив счет.
- **Файлы**: `apps/backend-api/src/billing/stripe.controller.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Использовать криптостойкие алгоритмы. Категорически запрещено выводить детали ошибки (Stack Traces) в HTTP ответ.

**Критерии завершения (DoD)**:
☐ Использовать `stripe.webhooks.constructEvent()` с проверкой подписи `STRIPE_WEBHOOK_SECRET` и Raw Buffer (не распарсенный JSON!).

☐ Сфабрикованный вебхук отбрасывается с ошибкой `400 Invalid Signature`.

---

### LOG-181

- **Название**: Переход на Pino Logger (Высокая производительность)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/main.ts`.
- **Почему это нужно**: Дефолтный логгер NestJS (через `console.log`) синхронный и медленный, потребляет CPU при больших нагрузках.
- **Файлы**: `apps/backend-api/src/main.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Установить `nestjs-pino` и `pino-http`.

☐ Логи пишутся асинхронно в формате JSON, не блокируя Event Loop.

---

### LOG-182

- **Название**: Structured Logging (JSON)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/logger/logger.module.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/backend-api/src/logger/logger.module.ts`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Настроить Pino так, чтобы логи в stdout были строго в JSON (`{ "level": "info", "msg": "...", "userId": "123" }`).

☐ Такие системы, как ELK (Elasticsearch/Logstash/Kibana) или Datadog могут легко парсить и фильтровать наши логи.

---

### LOG-183

- **Название**: Request ID Tracing (Correlation ID)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/common/middleware/request-id.middleware.ts`.
- **Почему это нужно**: В логах каша. Непонятно, какие логи принадлежат одному HTTP-запросу.
- **Файлы**: `apps/backend-api/src/common/middleware/request-id.middleware.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ При входе запроса генерировать `x-request-id` (UUID). Использовать `AsyncLocalStorage` (Node 16+) для прокидывания этого ID во все слои сервисов. Добавлять его в каждый вызов Logger.

☐ В логах можно сделать поиск по `requestId` и увидеть весь путь от Controller до базы данных.

---

### LOG-184

- **Название**: Прокидывание Trace ID в Worker (BullMQ)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/generations/generations.service.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/backend-api/src/generations/generations.service.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля generations.service.ts`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ При `queue.add()` класть текущий `requestId` в данные джоба. Воркер читает его и инициализирует свой Logger с этим контекстом.

☐ Единый сквозной трейс: от клика юзера на фронте -> до обработки микросервисом в фоне.

---

### LOG-185

- **Название**: Интеграция Sentry Performance Tracing
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/main.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/backend-api/src/main.ts`, `apps/frontend/sentry.client.config.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля sentry.client.config.ts`. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Включить `tracesSampleRate: 0.1` (10% трейсов) в Sentry. Обернуть медленные методы (вызовы OpenAI, тяжелые SQL) в `Sentry.startTransaction()`.

☐ В дашборде Sentry появляются графики с указанием, какой участок кода (bottleneck) тормозит запрос больше всего.

---

### LOG-186

- **Название**: Скрытие приватных данных (PII Redaction)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/logger/logger.module.ts`.
- **Почему это нужно**: В логи могут случайно попасть пароли, токены или номера карт при дампе Request Body.
- **Файлы**: `apps/backend-api/src/logger/logger.module.ts`
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Настроить в Pino маскировку `redact: ['req.headers.authorization', 'body.password', 'body.token']`.

☐ В лог-системе (которую могут читать джуниоры) никогда нет секретов `Bearer *****`.

---

### LOG-187

- **Название**: Мониторинг времени (Time-to-Generate)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/worker/src/processors/generation.processor.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/worker/src/processors/generation.processor.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Замерять время с момента взятия задачи до получения ответа от Replicate. Экспортировать как Histogram-метрику в Prometheus (`generation_duration_seconds`).

☐ Grafana показывает перцентили (P95, P99) скорости провайдера нейросети. Мы узнаем, если OpenAI начнет тупить.

---

### LOG-188

- **Название**: Настройка Uptime Kuma (или Datadog Synthetics)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: Внешняя инфраструктура.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: Внешняя инфраструктура
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Средний
- **Влияние**: Локальное улучшение модуля Внешняя инфраструктура. Снижает технический долг в изолированной области.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Поднять Uptime Kuma (или другой пингер), который каждые 30 секунд дергает `/api/v1/health`.

☐ При падении сервера администратору приходит звонок/СМС или пуш в Telegram в течение минуты.

---

### LOG-189

- **Название**: Мониторинг баланса сторонних API
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/worker/src/health/api-balance.cron.ts`.
- **Почему это нужно**: У нас могут закончиться деньги на счету OpenAI или Replicate, генерации остановятся, а мы узнаем об этом только от злых юзеров.
- **Файлы**: `apps/worker/src/health/api-balance.cron.ts`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Высокий
- **Влияние**: Критическое влияние на всю систему (Blast Radius: High). Предотвращает потенциальный даунтайм, утечку данных или сбои в биллинге.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Создать Cron-задачу (каждые 6 часов), которая дергает биллинг-апи провайдеров. Если баланс < $10 — шлет Critical alert в Slack/Telegram.

☐ Проактивный мониторинг финансов инфраструктуры.

---

### LOG-190

- **Название**: Логирование бизнес-метрик (Business Intelligence)
- **Описание**: Задача направлена на глубокую архитектурную модификацию компонента. Основной фокус: `apps/backend-api/src/billing/billing.service.ts`.
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `apps/backend-api/src/billing/billing.service.ts`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Низкий
- **Влияние**: Локальное улучшение модуля billing.service.ts`. Снижает технический долг в изолированной области.
- **Приоритет**: LOW
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Сделать логирование типа `event: 'USER_UPGRADED', plan: 'PRO', amount: 2000`. Эти логи отправляются в Mixpanel или Amplitude.

☐ Отдел маркетинга видит конверсии в реальном времени.

---

### DEP-191

- **Название**: Миграция с Docker Compose на Docker Swarm (или K8s)
- **Описание**: Инфраструктурная задача по настройке деплоя (CI/CD pipelines, Kubernetes, Terraform) и мониторинга (Observability).
- **Почему это нужно**: Обычный `docker-compose` не обеспечивает Zero-Downtime deploy на нескольких серверах.
- **Файлы**: `docker-compose.yml`, `deploy/swarm/`
- **Зависимости**: Нет
- **Сложность**: Extreme (Требует пары сеньор-разработчиков)
- **Оценка времени**: 6h
- **Риск**: Средний
- **Влияние**: Глобальное инфраструктурное изменение. Затрагивает все микросервисы и пайплайны CI/CD. Полностью меняет процесс доставки кода.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Адаптировать конфигурацию для `docker stack deploy` (или написать Kubernetes Deployments).

☐ Команда `docker service update` обновляет контейнеры по одному (Rolling Update), юзеры не замечают прерывания связи.

---

### DEP-192

- **Название**: Настройка Load Balancer / Reverse Proxy (Traefik / Nginx)
- **Описание**: Инфраструктурная задача по настройке деплоя (CI/CD pipelines, Kubernetes, Terraform) и мониторинга (Observability).
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `traefik.yml`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 4h
- **Риск**: Высокий
- **Влияние**: Глобальное инфраструктурное изменение. Затрагивает все микросервисы и пайплайны CI/CD. Полностью меняет процесс доставки кода.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Развернуть Traefik как Ingress Controller. Настроить автоматическое получение SSL-сертификатов (Let's Encrypt).

☐ Трафик маршрутизируется к API, Frontend и WebSockets корректно по HTTPS.

---

### DEP-193

- **Название**: Настройка Horizontal Pod Autoscaler (HPA) для Воркеров
- **Описание**: Инфраструктурная задача по настройке деплоя (CI/CD pipelines, Kubernetes, Terraform) и мониторинга (Observability).
- **Почему это нужно**: Ночью серверов нужно мало (хватит 1 воркера), днем при наплыве трафика нужно 20 воркеров.
- **Файлы**: Infrastructure
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 4h
- **Риск**: Средний
- **Влияние**: Глобальное инфраструктурное изменение. Затрагивает все микросервисы и пайплайны CI/CD. Полностью меняет процесс доставки кода.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Использовать KEDA (Kubernetes Event-driven Autoscaling). Скейлить поды воркера на основе длины очереди BullMQ в Redis.

☐ Система автоматически заказывает сервера в облаке и масштабируется, если в очереди больше 100 джобов.

---

### DEP-194

- **Название**: База Данных: Настройка Replication (Primary-Replica)
- **Описание**: Инфраструктурная задача по настройке деплоя (CI/CD pipelines, Kubernetes, Terraform) и мониторинга (Observability).
- **Почему это нужно**: Один PostgreSQL не справляется с чтением истории от 100,000 юзеров.
- **Файлы**: `packages/database/src/client.ts`
- **Зависимости**: Нет
- **Сложность**: Extreme (Требует пары сеньор-разработчиков)
- **Оценка времени**: 8h
- **Риск**: Низкий
- **Влияние**: Глобальное инфраструктурное изменение. Затрагивает все микросервисы и пайплайны CI/CD. Полностью меняет процесс доставки кода.
- **Приоритет**: LOW
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Развернуть Read Replica. В Prisma настроить `@prisma/extension-read-replicas`, чтобы тяжелые GET запросы (Marketplace, History) шли на реплику, а POST/PUT — на Primary.

☐ Распределение нагрузки БД.

---

### DEP-195

- **Название**: Автоматизация бекапов БД (PostgreSQL & MinIO)
- **Описание**: Инфраструктурная задача по настройке деплоя (CI/CD pipelines, Kubernetes, Terraform) и мониторинга (Observability).
- **Почему это нужно**: Дата-центр сгорит — проект умрет.
- **Файлы**: `scripts/backup.sh`, Crontab
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 3h
- **Риск**: Высокий
- **Влияние**: Глобальное инфраструктурное изменение. Затрагивает все микросервисы и пайплайны CI/CD. Полностью меняет процесс доставки кода.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Написать скрипт `pg_dump`, заливающий архив в удаленное S3 хранилище (например, AWS или другой провайдер) каждую ночь в 03:00.

☐ Проверенное и гарантированное восстановление базы из бекапа.

---

### DEP-196

- **Название**: Защита от DDoS на уровне DNS (Cloudflare)
- **Описание**: Инфраструктурная задача по настройке деплоя (CI/CD pipelines, Kubernetes, Terraform) и мониторинга (Observability).
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: Cloudflare Dashboard
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Глобальное инфраструктурное изменение. Затрагивает все микросервисы и пайплайны CI/CD. Полностью меняет процесс доставки кода.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Включить проксирование (Оранжевое облако). Настроить WAF (Web Application Firewall) правила: блокировка подозрительных ботов (Bot Fight Mode).

☐ Синтетические L7 DDoS атаки блокируются до того, как достигнут нашего сервера.

---

### DEP-197

- **Название**: CI/CD: Настройка Environments (Staging & Prod)
- **Описание**: Инфраструктурная задача по настройке деплоя (CI/CD pipelines, Kubernetes, Terraform) и мониторинга (Observability).
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `.github/workflows/deploy-staging.yml`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 4h
- **Риск**: Высокий
- **Влияние**: Глобальное инфраструктурное изменение. Затрагивает все микросервисы и пайплайны CI/CD. Полностью меняет процесс доставки кода.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Ветка `develop` автоматически деплоится на `staging.arrena.ai`. Ветка `main` деплоится на `arrena.ai` после ручного аппрува (Manual Approval) в GitHub Actions.

☐ QA инженер может тестировать фичи на отдельном сервере со своей БД, не трогая реальных пользователей.

---

### DEP-198

- **Название**: Нагрузочное тестирование (Load Testing)
- **Описание**: Инфраструктурная задача по настройке деплоя (CI/CD pipelines, Kubernetes, Terraform) и мониторинга (Observability).
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `tests/load/k6-script.js`
- **Зависимости**: Нет
- **Сложность**: High
- **Оценка времени**: 5h
- **Риск**: Средний
- **Влияние**: Глобальное инфраструктурное изменение. Затрагивает все микросервисы и пайплайны CI/CD. Полностью меняет процесс доставки кода.
- **Приоритет**: MEDIUM
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Написать скрипт на базе `k6`. Эмулировать 500 одновременных пользователей, авторизующихся и нажимающих "Generate".

☐ Отчет `k6` доказывает, что система держит 1000 RPS (Requests per second) без потери соединений и 500 ошибок.

---

### DEP-199

- **Название**: Удаление Hardcoded IP-адресов
- **Описание**: Инфраструктурная задача по настройке деплоя (CI/CD pipelines, Kubernetes, Terraform) и мониторинга (Observability).
- **Почему это нужно**: При масштабировании IP адреса узлов меняются динамически.
- **Файлы**: Весь проект
- **Зависимости**: Нет
- **Сложность**: Low (Рутинный рефакторинг)
- **Оценка времени**: 1h
- **Риск**: Высокий
- **Влияние**: Глобальное инфраструктурное изменение. Затрагивает все микросервисы и пайплайны CI/CD. Полностью меняет процесс доставки кода.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Частично (LLM-агент может написать код, но миграция схемы БД и apply terraform должны быть мануальными)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Заменить везде жесткие IP на DNS имена сервисов (например, `redis:6379`, `postgres:5432`, `http://backend-api:4000`).

☐ Инфраструктура полностью Portable.

---

### DEP-200

- **Название**: Финальный Production Release Sign-off
- **Описание**: Инфраструктурная задача по настройке деплоя (CI/CD pipelines, Kubernetes, Terraform) и мониторинга (Observability).
- **Почему это нужно**: Архитектурный компонент нуждается в срочном рефакторинге для соответствия Enterprise-стандартам (устранение спагетти-кода, анти-паттернов проектирования и снижение цикломатической сложности).
- **Файлы**: `PRODUCTION_CHECKLIST.md`
- **Зависимости**: Нет
- **Сложность**: Medium
- **Оценка времени**: 2h
- **Риск**: Высокий
- **Влияние**: Глобальное инфраструктурное изменение. Затрагивает все микросервисы и пайплайны CI/CD. Полностью меняет процесс доставки кода.
- **Приоритет**: HIGH
- **Возможность автоматического исправления**: Нет (Требует вдумчивого ручного код-ревью и понимания бизнес-логики)
- **Рекомендации по реализации**: Следовать принципам SOLID. Обязательно покрыть все новые ветвления Unit-тестами (Jest) перед созданием Pull Request.

**Критерии завершения (DoD)**:
☐ Сгенерировать, заполнить и проставить галочки во всех пунктах документа `PRODUCTION_CHECKLIST.md`.

☐ Система готова к приему 1,000,000 пользователей. Запуск рекламной кампании.

---

