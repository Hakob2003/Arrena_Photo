# Отчет о выполнении задачи: Обновление роутинга в разделе Billing

✅ **Задача успешно выполнена.**

### Что было сделано:
1. **Проверка констант и макета:** Убедился, что `constants.ts`, `registry.ts` и `layout.tsx` (совместно с `BillingVirtualCylinder.tsx`) уже используют систему хэшей (`#overview`, `#plans` и т.д.).
2. **Единая страница `/billing`:** 
   - Полностью переписал `app/billing/page.tsx`. Теперь компонент корректно читает хэш из URL и рендерит нужную вкладку для пользователей без премиум-скина.
   - Удалил старые вложенные папки (`overview`, `payment`, `plans`, `usage`), так как роутинг теперь происходит в рамках одной страницы.
3. **Обновление компонента OverviewTab:**
   - Изменил логику кнопки смены тарифа (Change Plan). Теперь, если пропс `onNavigateToPlans` не передан, кнопка автоматически меняет `window.location.hash = '#plans'`, что обеспечивает плавную навигацию внутри приложения.

### Результат:
- "Барабан" (Virtual Cylinder) корректно переключает вкладки без перезагрузки маршрутов.
- Поддерживается прямое открытие ссылок (например, `.../billing/#usage`).
- Код очищен от неиспользуемых папок и устаревших перенаправлений.

Если все отлично, мы можем переходить к следующим задачам!
